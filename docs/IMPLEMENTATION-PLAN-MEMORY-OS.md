---
version: "0.1.18b"
created_at: "2026-09-14T12:00:00+07:00,Claude Opus 5,working-tree"
last_update: "2026-09-16T09:00:00+07:00,ATHER"
status: "proposed"
attributes:
  domain: "mission-state-protocol"
  doc_type: "implementation-plan"
  scope: "PLAN-MSP-MEMOS — MSP as a complete multi-user, multi-agent memory runtime: phases, sprints, backlog, gates, decisions, risks"
---

# PLAN-MSP-MEMOS — Implementation plan: MSP memory OS (multi-user, multi-agent)

## สรุปภาษาไทย

แผนนี้ทำให้ MSP เป็นระบบความจำที่สมบูรณ์ รองรับหลายผู้ใช้ (principal) และหลาย agent โดย**ยังไม่เชื่อมกับ LINE OA** ตามคำสั่งเจ้าของเมื่อ 2026-09-14 งานแบ่งเป็นสามระดับ และทุกรายการมี id กำกับ

- **Phase** (`PH-MEMOS-*`) เป็นขั้นตอนใหญ่ แต่ละ phase ต้องผ่าน gate ของตัวเองก่อนไปต่อ
- **Sprint** (`SPR-MEMOS-*`) คือรอบการส่งมอบ หนึ่ง sprint ประกอบด้วยงานที่รีวิวและ merge ร่วมกัน
- **Backlog item** (`BL-MEMOS-*`) คืองานย่อยที่ลงมือทำได้จริง แต่ละรายการระบุ epic, phase, sprint, ผู้รับผิดชอบ, สิ่งที่ต้องทำก่อน และหลักฐานที่ต้องแสดงก่อนปิดงาน

Epic ใช้ id `TASK-MEMOS-001..010` เดิมจาก roadmap ของ zuri-ai (PR #380) เพื่อให้สองเอกสารอ้างถึงกันได้

ลำดับงานมีดังนี้

1. **PH-MEMOS-0** รากฐาน (เสร็จแล้ว)
2. **PH-MEMOS-1** บันทึกการตัดสินใจและ contract
3. **PH-MEMOS-2** thread memory ที่ปลอดภัยต่อหลายผู้ใช้
4. **PH-MEMOS-3** multi-agent
5. **PH-MEMOS-4** participant lifecycle และการลบข้อมูล
6. **PH-MEMOS-5** vault รายบุคคลและ `msp_vault_resolve`
7. **PH-MEMOS-6** consolidation และ passport
8. **PH-MEMOS-7** hardening และ release
9. **PH-MEMOS-8** เชื่อมช่องทางจริง (เลื่อนออกไปตามคำสั่งเจ้าของ)

## 1. Purpose and scope

**Goal.** MSP becomes a complete memory runtime in which:

- every read and write is scoped to one tenant;
- a person's private memory is visible only to that person's own authorised context;
- two agents serving the same person keep separate episodic memory and share only what the model explicitly shares;
- all of the above is proven by suites that run through the real `msp-server` process.

**In scope.** MSP repository work only:

- the thread/session surface (API-011);
- principal vaults and `msp_vault_resolve` (API-010);
- the API-009 caller-identity amendment;
- participant and agent lifecycle;
- erasure and retention;
- consolidation;
- migration-runner follow-ups;
- the release.

**Out of scope for now.** Everything that needs a channel. Real-process LINE proof, the LINE canary and production summary hosting are `TASK-MEMOS-005..007`, parked in PH-MEMOS-8 by owner direction (2026-09-14). The same applies to any zuri-ai code change.

**Sources this plan is built from.**

- [`DESIGN-SESSION-EPISODIC-INSTANCE-MEMORY.md`](DESIGN-SESSION-EPISODIC-INSTANCE-MEMORY.md), now at v0.3.5b after three RKOI NEEDS REVISION rounds (v0.3.0b, v0.3.1b, v0.3.2b), one APPROVED-with-warnings docs round (v0.3.3b → v0.3.4b) and one stage-1 code-review round (v0.3.4b → v0.3.5b) (BL-MEMOS-011, BL-MEMOS-012); each round reads the shipped stage-1 code (and, from round three on, zuri-ai's own real signer) more directly than the last as the source of truth for wire/schema shapes.
- KIN's stage-1 port, `feat/memos-002-thread-memory` (worktree `agent-ab508b7a790efd268`): `migrations/0008_thread_memory.sql`, `packages/msp-core/src/domain/thread-memory.mjs`, `packages/msp-contracts/src/contracts/thread-access.mjs`, `apps/msp-server/src/transport/handlers/thread-guard.mjs`, `docs/API-011-THREAD-MEMORY-CONTRACT.md`.
- RKOI's own review findings (rounds one through four), each backed by direct inspection of zuri-ai's real code and the shipped migration; per RKOI's own instruction, this plan cites the findings and the backlog items that prove them, not the session-scratch scripts RKOI used to derive them.
- RKOI's clause-by-clause comparison (2026-09-14) of that design against the unmerged branch `codex/msp-thread-memory` (`50859fb`, `e4303cb`).
- RKOI's reviews of MSP PRs #16–#19.
- zuri-ai `docs/roadmap/PLAN-MSP-MEMORY-OS-LINE-AGENT.md` (PR #380).

## 2. Identifier scheme

Every item carries exactly one id. Ids are keys: never renumber or reuse them. Mark a dropped item `cancelled` and keep its number burnt, following the zuri-ai convention in AGENTS.md §18.

| Family | Form | Meaning |
|---|---|---|
| Plan | `PLAN-MSP-MEMOS` | this document |
| Phase | `PH-MEMOS-<n>` | a stage with an exit gate |
| Gate | `GATE-MEMOS-<n>` | exit criteria of phase `n` |
| Sprint | `SPR-MEMOS-<nn>` | one delivery increment, ending in reviewed merges |
| Epic | `TASK-MEMOS-<nnn>` | the ten roadmap items in zuri-ai `ROADMAP.md` (rev 2.74.0b) |
| Backlog item | `BL-MEMOS-<nnn>` | an implementable unit with an owner, dependencies and proof |
| Decision | `DEC-MEMOS-<nn>` | an owner decision; DEC-MEMOS-01..16 confirmed by the owner on 2026-09-14 |
| Risk | `RSK-MEMOS-<nn>` | a tracked risk with a mitigation |

BL numbers are grouped by phase: 001–009 for phase 0, 010–019 for phase 1, 020–039 for phase 2, and so on. Gaps are reserved for items discovered inside that phase. **Where a phase's block is already fully allocated (2026-09-14 revision: phase 3's 040–049 has no free slot), a newly discovered item takes the next free number in the 100+ range instead of renumbering anything**; its Epic/phase columns still say which phase it belongs to. This is itself a convention change, recorded in this revision's CHANGELOG row rather than applied silently.

**Status vocabulary:** `done`, `in-progress`, `review`, `planned`, `blocked`, `deferred`, `cancelled`.

**Owners** follow the repo roster in `CLAUDE.md`:

| Owner | Responsibility |
|---|---|
| KIN | code |
| JANUS | migrations, packaging |
| GHOST | suites |
| ATHER | documents |
| RKOI | review gate |
| OWNER | decisions |
| COORD | commits, PRs, integration |

## 3. Phases

| Phase | Epics | Goal | Exit gate | Status |
|---|---|---|---|---|
| PH-MEMOS-0 | — | Foundations: migration runner safe for parent rebuilds, first design, roadmap | GATE-MEMOS-0 | done |
| PH-MEMOS-1 | TASK-MEMOS-001 | Decision record and design (current: v0.3.5b) around API-011, including the multi-user/multi-agent model | GATE-MEMOS-1 | in-progress |
| PH-MEMOS-2 | TASK-MEMOS-002 (stage 1) | Thread memory on `main`, safe for many users: C-1/C-2 closed, tenant-scoped, pseudonymous journal | GATE-MEMOS-2 | in-progress |
| PH-MEMOS-3 | TASK-MEMOS-002 (stage 2) | Multi-agent: agent identity in the grant, thread agents, per-agent record visibility | GATE-MEMOS-3 | planned |
| PH-MEMOS-4 | TASK-MEMOS-003, TASK-MEMOS-004 | Participant and agent lifecycle; erasure, retention and export for thread tables | GATE-MEMOS-4 | planned |
| PH-MEMOS-5 | TASK-MEMOS-008 | Principal vaults, API-010 `msp_vault_resolve`, API-009 access context, scoped context receipts | GATE-MEMOS-5 | planned |
| PH-MEMOS-6 | TASK-MEMOS-009 | Consolidation into principal vaults, passport, cross-thread recall, vault erasure | GATE-MEMOS-6 | planned |
| PH-MEMOS-7 | TASK-MEMOS-010 | Hardening and release: runner follow-ups, end-to-end multi-user/multi-agent acceptance, Gate A re-baseline, client release | GATE-MEMOS-7 | planned |
| PH-MEMOS-8 | TASK-MEMOS-005, TASK-MEMOS-006, TASK-MEMOS-007 | Channel activation (LINE OA): real-process proof, canary, production summaries | GATE-MEMOS-8 | deferred (owner direction 2026-09-14) |

### Exit gates

Every gate also requires:

- `npm test`, meaning `test:vitest` plus `test:security`;
- `npm run test:integration`, including `gks-provider-bridge.test.mjs`;
- `dependency-boundaries.test.mjs` passing;
- RKOI APPROVED with 0 critical;
- the updated rows in `docs/GATE-A.md`, `docs/NOTES.md` and `docs/MIGRATION.md` that ATHER's checklist requires.

- **GATE-MEMOS-0 (met)**
  - MSP PRs #15–#19 merged with CI green on Node 22 and 24.
  - Zuri-ai PR #380 merged.
- **GATE-MEMOS-1**
  - The ADR (current: v0.1.5b) and design (current: v0.3.5b) are merged.
  - DEC-MEMOS-01..16 are recorded as confirmed by the owner (2026-09-14).
  - The API-011 tool shapes are fully specified, with no field zuri-ai sends changed except those named in RSK-MEMOS-01.
- **GATE-MEMOS-2** — every case below runs in `tests/security/thread-memory-scoping.security.mjs` (design §15) unless noted:
  - Migration 0008 is on `main`, and the real-graph migration tests (fresh and populated) pass.
  - Each C-1 attack is reproduced and refused through the real process: a second person joining, an UNKNOWN-kind bypass, a PENDING self-upgrade without the DEC-MEMOS-15 conditions, and a planted record.
  - DEC-MEMOS-15's self-upgrade succeeds under its full condition set (incoming request and stored row) and is refused, or silently ignored for the downgrade case, the instant any one condition does not hold.
  - **A grant scoped to room R1's hash is refused against a thread that only shares R1's tenant/business/channel-account, including via `msp_session_compaction_claim`/`commit`/`retry` resolving through the job's own thread (`BL-MEMOS-111`).**
  - **A grant with no room claim at all (`externalRoomRef` or `channelAccountId` absent) is refused `thread_scope_denied` on every thread-bound tool — `context`, `append`, `memory_record`, `injection`, `delivery`, `claim`, `commit`, `retry` — never allowed to pass on tenant/business/account alone (`BL-MEMOS-111`, CRITICAL on the code, KIN fixing it).**
  - `audienceKind` is required and enforced on `resolve`/`append`/`context`/`memory_record`/`injection_record`; `msp_thread_delivery_record` is the one exempt tool, scoped instead by the inbound message's thread and the room hash.
  - A `msp_thread_resolve` whose `channel_type` differs from an existing `ACTIVE` thread's stored `channel_type`, for the same tenant/account/room hash, is refused `conflict` (DEC-MEMOS-16).
  - `msp_session_sweep` is refused when its grant carries no room claim, exactly like the other seven thread-bound tools — it is room-scoped, not tenant-scoped.
  - The C-2 decoupling assertion passes (`dependency-boundaries.test.mjs`).
  - W1, W5, W6, W7 and W10 are proven.
  - `tests/cross/zuri-thread-contract.test.mjs` passes via `npm run test:cross-zuri` against a read-only extract of zuri-ai `origin/main` (`BL-MEMOS-110`). **`test:cross-zuri` matches zuri-ai's current stage, stated once here and not re-derived per gate (RKOI stage-2 review round 2, finding 7, still open from round 1's warning 6)**: before `BL-MEMOS-107` lands, the suite asserts zuri-ai's unmodified grant is refused with a typed grant error; after `BL-MEMOS-107` lands, the same suite must pass unmodified. Both `GATE-MEMOS-2` and `GATE-MEMOS-3` name this **same** suite and must both stay meetable against whatever `main` currently is — once stage 2 merges, `GATE-MEMOS-2`'s own re-run of this bullet reflects stage 2's rules too, not a frozen stage-1-only expectation that would otherwise spuriously fail the moment stage 2 lands.
- **GATE-MEMOS-3** — every case below runs in `tests/security/thread-agent-scoping.security.mjs` unless noted (design §8, §15):
  - Agent B cannot read agent A's `AGENT`-visibility protected records; a `THREAD`-visibility record and every legacy (`agent_id IS NULL`) row are visible to both. Agent B recording content identical to agent A's `AGENT`-visibility record gets its **own** distinct record, not A's (`record_id`'s hash includes `agent_id`/`visibility`). **Superseding an unknown id, B naming A's `AGENT`-visibility record, and a record failing the pre-existing stage-1 ownership/status check are one identical answer** — `validation_failed` with the single fixed message "supersedes_record_id does not name a record this caller can supersede" — never three distinguishable codes and never stage 1's own `conflict` for the ownership/status case (corrected, RKOI stage-2 review round 2, superseding round 1's own `thread_scope_denied` fix, which was itself a third oracle value). `THREAD`-visibility and legacy `agent_id IS NULL` records stay supersedable by any agent under the stage-1 rules alone (CRITICAL 2, RKOI stage-2 review rounds 1–2).
  - A departed agent is denied `agent_not_current` on its very next call, on every thread-bound tool — `append`, `context`, `memory_record`, `injection_record`, `delivery_record`, `claim`, `commit`, `retry` — not merely `context`. **This is a per-call property of the agent gate, not a revocation control** (`DEC-MEMOS-18`) — the same departed agent can re-attach on its own `assertAgents` the very next call.
  - No agent can act on a thread it is not attached to (no `thread_agents` row at all, current or departed) — refused `agent_not_current` identically to a departed agent.
  - Attachment happens only two ways: auto-attach on the calling agent's own `created: true` resolve, or `assertAgents === true` against an existing thread; a non-current agent's resolve without `assertAgents` is `agent_not_current`. Two concurrent resolves for the same room produce exactly one auto-attached agent (the thread `INSERT` winner) — the loser never auto-attaches merely because its own lookup ran first (mint-race, RKOI stage-2 review round 1, item 9).
  - **CRITICAL 1, corrected (RKOI stage-2 review round 1)**: the delivery pending path (no inbound message yet) requires the calling agent be current on the room's `ACTIVE` thread, refusing `agent_not_current`, or `not_found` if the room has no `ACTIVE` thread at all; at drain time the stored agent is re-checked, and a departed agent's row is left unreconciled (`reconcile_state` stays `'pending'`; the already-shipped `msp_thread_message_append.reconcile_skipped` journal entry, `RSK-MEMOS-09`, now also records `error_code: 'agent_not_current'`) rather than minting the `OUTBOUND` reply under a fixed `speakerId`.
  - The journal actor is `grant.agentId` in plain text for agent-attributable entries (not a W5 regression — `agentId` is not personal data), and no raw principal id appears anywhere in the journal.
  - A replayed grant on any nonce-required tool (every mutating tool except `msp_thread_message_append`, and excluding the read-only `msp_thread_context`) is refused `grant_replayed`; a nonce-required tool called with no `nonce` claim is `grant_nonce_required`; a replayed nonce's transaction (mutation + nonce insert) rolls back together, never partially applying.
  - **`DEC-MEMOS-18`, revised**: the worker attaches via `assertAgents`, never mints a thread from a worker-only grant (`operator`, no reader/writer flags) — `not_found` if the room has no `ACTIVE` thread; `msp_session_compaction_claim`/`commit`/`retry` refuse `agent_not_current` unless the worker's own `grant.agentId` is a current agent of the job's resolved thread; `msp_session_sweep` is never refused for "not current" but is refused if `agentId`/`workspaceId` are absent.
  - `requesterAgentId` absent in stage 2 sees `THREAD` records only, never falls through to showing every `AGENT` record.
  - **`DEC-MEMOS-17`**: zuri-ai's current, unmodified grant (no `agentId`/`workspaceId`/`nonce`) is refused with a typed grant error — proven via `test:cross-zuri`, which the design and this plan state precisely: **before `BL-MEMOS-107` lands, `test:cross-zuri` asserts zuri-ai's `origin/main` grant is refused with a typed grant error; after `BL-MEMOS-107` lands, the same suite must pass against zuri-ai's updated port** — this is `test:cross-zuri`'s named suite for `GATE-MEMOS-2` as well as here, and the flip is a deliberate, expected change to what "green" means for that one suite, not a regression. The "wrapped port" case proves only that MSP *accepts the shape*, not that zuri-ai has actually adopted it — labelled that way in the suite and in `BL-MEMOS-047`'s acceptance.
  - **Keyring cases move to their own suite, `tests/security/thread-service-keyring.security.mjs`** (RKOI stage-2 review round 1, warning 1) — not this file. See `BL-MEMOS-049`.
- **GATE-MEMOS-4** — suites as named by `BL-MEMOS-052` and `BL-MEMOS-056`, against design v0.5.3b §7.1/§8.6/§11.2 (fully scoped, unstarted, RKOI APPROVED round 4). **Every bullet below reflects the spec as it stood after round 4, not any earlier round's text:**
  - **Relink**: `close_for_relink` denies the old `DIRECT` thread including pre-departure history — a relinked or departed principal's grant is refused on the old thread's every tool, and the new thread the next `msp_thread_resolve` mints for the same binding is empty of the old one's messages/records/summaries. `close_for_relink` without both `assertParticipants` and the new `assertRelink` claim (`DEC-MEMOS-23`) is `thread_scope_denied`; the same call on a `GROUP`/`ROOM` thread is `thread_scope_denied` too. **The race's remaining raw-driver-error case is closed**: the interleaving that surfaces `SQLITE_BUSY_SNAPSHOT` from the store's re-check is asserted to reach the caller only as a typed `conflict`, never the raw SQLite code/message (`tests/security/participant-lifecycle-relink.security.mjs`, `BL-MEMOS-052`).
  - **Leave**: `leave` without `assertParticipants` is `thread_scope_denied`, self or third-party (`DEC-MEMOS-22`); naming a `speaker_id` with no open row is `not_found`; `leave` never flips `threads.status`. **A departed principal cannot silently rejoin by appending again (`CRITICAL 1`, `BL-MEMOS-058`, re-specified round 2 as an explicit three-way branch, not a modified two-branch condition — round 1's own fix was under-specified and both literal readings of it failed)**: the guard now distinguishes (1) never-participated, (2) participated-but-none-current (a rejoin — **unconditionally** requires `assertParticipants`, **never** reads `current` at all, **never** falls through to `DEC-MEMOS-15`), and (3) a current row exists (today's unchanged logic). The principal's very next plain `HUMAN` append after `leave` is refused `thread_scope_denied` (case 2), never a claim-free re-creation of membership — direct-`SELECT` HUMAN participant count for the thread stays `0` across the refused append, and a call carrying `assertParticipants` still succeeds, including for a *different, departed third party* on a `GROUP`/`ROOM` thread (**case 2b, new**: an intended, defensible consequence of case 2's condition being keyed on `speakerId` alone, not `speakerId === grant.principalId` — refused unconditionally on `DIRECT` threads by the existing single-HUMAN schema constraint, §6.3). **A current row's `DEC-MEMOS-15` self-upgrade with no claim still succeeds (case 3b)** — regression-proofed as its own required case, since a naive fix could have broken it too (`tests/security/participant-lifecycle-relink.security.mjs`, `BL-MEMOS-052`).
  - **Detach**: a detached agent (`msp_thread_agent_detach`) is denied `agent_not_current` on every thread-bound tool on its very next call, and can re-attach via its own `assertAgents` (`tests/security/participant-lifecycle-relink.security.mjs`, `BL-MEMOS-052`).
  - **Erasure leaves no *content* of the erased principal and is idempotent (corrected wording, `DEC-MEMOS-28`'s pinned-id convention — not "removes all traces," since raw ids are pinned by design)**: after `msp_thread_principal_erase`, direct-database assertions after `close()` show `redaction_state = 'tombstoned'` and a blanked content column (including `protected_memory_records.scope_json`, not only `body_json` — `CRITICAL 4` item 1, requires migration `0010`) for every qualifying row in `thread_messages`/`protected_memory_records`/`session_summaries`/`thread_delivery_receipts`, and every open `thread_participants` row for that principal is closed; every tool (`context`, `export`) is blind to the erased content. **`thread_pending_deliveries` is asserted untouched by erasure** (row count and content unchanged — `CRITICAL 4` item 2, it holds AGENT-authored reply text, not the principal's own, and is out of scope). **Ordering, corrected round 2 (WARNING 1)**: `thread_participants` rows close only after every content-table tombstone has run in the same transaction, kept as defense in depth for a future `left_at`-filtered query — proven as a **property test**, both orderings run separately and asserted to produce an identical result against the `DEC-MEMOS-34` query as actually specified (no `left_at` filter, so today's result is honestly ordering-independent, not "would break" as round 1 wrongly claimed). A second call with the same `(tenant_id, idempotency_key)` and the same `principal_id` returns the stored receipt with **no further writes** (confirmed by an unchanged row count, not merely a successful response); the same key with a *different* `principal_id` is `conflict` (`tests/security/thread-erasure.security.mjs`, `BL-MEMOS-056`).
  - **`DEC-MEMOS-34` — sole-ever-HUMAN-participant scoping, both directions, required reproduction (`CRITICAL 2/3`), corrected round 2 with a second independent disqualifying condition (`CRITICAL 2`, new)**: on a `GROUP` thread where a second principal (`p2`) authored the summarized content and `p1` authored none of it, (a) `msp_thread_principal_erase` for `p1` leaves that thread's `session_summaries`/`thread_delivery_receipts` rows completely untouched (`redaction_state` stays `'none'`, content unchanged, confirmed by a direct re-`SELECT`); (b) `msp_thread_principal_export` for `p1` on the same thread returns no summary from it at all — not redacted, not partial, simply absent. **New, required (`CRITICAL 2`) — the UNKNOWN-speaker reproduction**: a `GROUP` thread with exactly one sole-ever `HUMAN` participant (`p1`) that also carries one `UNKNOWN`-speaker message from an unresolved second person is likewise excluded from both erasure and export, even though the participant-only test alone would have wrongly qualified it — `AGENT`-authored messages never disqualify (the assistant's own reply), `UNKNOWN`/`OPERATOR` always do. **New, required (WARNING 4)**: a thread with one shared `speaker_id` used by two different `person_id`s (schema-legal) is disqualified by the `person_id` count alone, proving the check is two separate counts, not a `speaker_id`-only one (`tests/security/thread-erasure.security.mjs`, `BL-MEMOS-056`).
  - **Retention refuses cross-tenant, and is deliberately whole-tenant not room-scoped (stated as a ruling, not a gap)**: `msp_thread_retention_tick` only ever touches `grant.tenantId`'s own rows, by construction (no tenant parameter exists to name a different one) — an operator grant issued with room-specific claims (for compaction/sweep) still correctly sweeps the *whole* tenant when used for retention, since retention is a tenant-level compliance sweep, not a per-room operation. **`dry_run` assertion, corrected round 2 (WARNING 2) — row counts cannot distinguish a dry run from a live pass (both use `UPDATE`, never `DELETE`)**: asserted instead is `COUNT(*) WHERE redaction_state != 'none'` being `0` after `dry_run: true` and non-zero after an otherwise-identical `dry_run: false`, plus the tombstoned content columns unchanged after dry and blanked after live. **`dry_run: true` consumes no nonce but now also writes a journal entry (`DEC-MEMOS-35`, revised round 2, WARNING 6) — only the nonce exemption is dry-run-specific**, asserted on both arms (`tests/security/thread-erasure.security.mjs`, `BL-MEMOS-056`).
  - **Export contains only the requester's own material and nothing tombstoned or foreign**: `msp_thread_principal_export` contains only the requesting/named principal's own authored `thread_messages` and own asserted/subject `protected_memory_records` always; it contains `session_summaries` only for threads where that principal was the thread's sole-ever `HUMAN` participant with no disqualifying `UNKNOWN`/`OPERATOR` message (`DEC-MEMOS-34`) — never another principal's, never another `AGENT`-visibility asserter's thread-unrelated content — and nothing tombstoned, including the exporting principal's own previously-erased content (`DEC-MEMOS-30`). Self-erase/export needs `dataSubjectAccess`; naming another principal without `dataSubjectAdmin` is `thread_scope_denied` (`DEC-MEMOS-25`) (`tests/security/thread-erasure.security.mjs`, `BL-MEMOS-056`).
- **GATE-MEMOS-5** — every case below runs in `tests/security/principal-vault-scoping.security.mjs` plus `provenance-ids-are-not-owners.security.mjs` and `context-tools-ownership.security.mjs` unless noted (design v0.6.0b §15, fully scoped, unstarted):
  - A wrong tenant, principal, agent or workspace is denied on every new tool and on all nine `msp_memory_*` tools, including the entity-id-only ones (`msp_memory_history`/`forget`/`links_list`, resolved via the entity's own `vault_id`; `msp_memory_links_create`, resolved via `from_entity_id`'s vault under the pre-existing same-vault-as-`to_entity_id` refusal); a wrong or missing `allow_passport` on a `principal_passport` target is `access_context_denied`, the same code as a tuple mismatch, deliberately; `access_context` entirely absent on a principal-vault-type request is the distinct `access_context_required`, never conflated with `access_context_denied`.
  - Provenance ids (`instance_id`/`thread_id`/`session_id`, wherever they appear) never widen vault scope — a correct `tenant_id`/`principal_id`/`agent_id`/`workspace_id` authorizes identically regardless of what provenance fields are present or absent.
  - Principal vaults are never mountable — proven independently at both layers: `msp_vault_mount` refused at the `VaultRegistry#mountVault` JS check, and a direct `INSERT`/`UPDATE` against `vault_mounts` refused by `0011`'s own DB triggers even with the JS layer bypassed.
  - The same resolve returns the same vault, including under a genuine two-process race (the losing `INSERT` hits the partial unique index and re-`SELECT`s, never a raw `SqliteError` or a second row).
  - `msp_memory_decay_tick`'s `pinned` field is `true` only for a `principal_passport` vault, with `evaluated: 0`/`transitioned: []` on both `dry_run` arms; `false` and normal decay for `principal_private`.
  - Scoped `contexts` rows need a matching access context on `msp_context_diff`/`audit`/`replay`; a legacy (unscoped) row is unaffected; `include_payload` is refused for a scoped row even given a correctly-matching access context.
  - The principal-vaults migration (`0011`, number assigned at merge per `DEC-MEMOS-14`/`37` — concrete now that `0008`–`0010` are confirmed merged) migrates fresh and populated (through `0010`) databases, using the `-- msp-migration: foreign-keys=off` directive; `vault_mounts`/`entities`/`promotions`/`links`'s `REFERENCES vaults` still name `vaults` post-rebuild; an unexpected `vaults.status` row is refused. The scoped-`contexts` migration (`0012`) applies cleanly on top.
  - Cross-repo: `msp_vault_resolve`'s response satisfies zuri-ai's shipped `validateVaultSet` unchanged when called exactly as `msp-vault-resolver.js` calls it (`BL-MEMOS-105`).
  - Nothing in this surface calls GKS with no configured provider; a principal vault's own entities remain eligible for the existing `msp_memory_promote` path with no special-casing (extended `shared-scope-fail-closed.security.mjs`).
- **GATE-MEMOS-6** — every case below runs in `tests/security/consolidation-vault-scoping.security.mjs`, `cross-thread-digest-scoping.security.mjs` and `group-thread-private-context.security.mjs`, plus the extended `erasure-invalidates-retrieval.security.mjs` (suites as named by `BL-MEMOS-074`):
  - No fact lands in a vault that a direct upsert under the same context would be denied.
  - A group thread never leaks into a private context.
  - Two agents serving one person keep separate episodic vaults.
  - Erasure extends to entities, history, FTS and embeddings.
- **GATE-MEMOS-7**
  - The end-to-end acceptance suite (2 tenants × 3 principals × 2 agents × DIRECT/GROUP) passes through the real process.
  - Gate A is re-baselined.
  - `pack:client` is clean.
  - DEC-MEMOS-01..16 are confirmed by the owner (met 2026-09-14), and any later DEC is confirmed or amended and re-reviewed.
- **GATE-MEMOS-8 (deferred)**
  - The zuri-ai PLAN exit gates, re-opened by the owner.

## 4. Sprints

Sprints are nominally one week. Dates are planning targets, not commitments. A sprint ends when its items are merged, and it does not end on the calendar date if the RKOI gate has not passed.

| Sprint | Target window | Phases | Sprint goal | Backlog items |
|---|---|---|---|---|
| SPR-MEMOS-00 | ≤ 2026-09-14 | PH-MEMOS-0 | Foundations merged | BL-MEMOS-001..006 |
| SPR-MEMOS-01 | 2026-09-14 → 09-20 | PH-MEMOS-1, PH-MEMOS-2, PH-MEMOS-7 | Decision record under review; thread memory port and C-1/C-2 fixes written; small runner follow-ups | BL-MEMOS-010..014, BL-MEMOS-020..032, BL-MEMOS-080..082 |
| SPR-MEMOS-02 | 09-21 → 09-27 | PH-MEMOS-2 | Thread memory merged on `main` (GATE-MEMOS-2) | BL-MEMOS-033 plus any RKOI fix-ups |
| SPR-MEMOS-03 | 09-28 → 10-04 | PH-MEMOS-3 | Multi-agent merged (GATE-MEMOS-3) | BL-MEMOS-040..049 |
| SPR-MEMOS-04 | 10-05 → 10-11 | PH-MEMOS-4 | Lifecycle and thread erasure merged (GATE-MEMOS-4) | BL-MEMOS-050..057 |
| SPR-MEMOS-05 | 10-12 → 10-18 | PH-MEMOS-5 | Vault types, `msp_vault_resolve`, API-009 amendment | BL-MEMOS-060..063, BL-MEMOS-067 |
| SPR-MEMOS-06 | 10-19 → 10-25 | PH-MEMOS-5 | Scoped context receipts, multi-agent vault rules, suites; merged (GATE-MEMOS-5) | BL-MEMOS-064..066, BL-MEMOS-068 |
| SPR-MEMOS-07 | 10-26 → 11-01 | PH-MEMOS-6 | Consolidation, passport, cross-thread recall, vault erasure (GATE-MEMOS-6) | BL-MEMOS-070..075 |
| SPR-MEMOS-08 | 11-02 → 11-08 | PH-MEMOS-7 | Acceptance, re-baseline, client release, owner confirmation (GATE-MEMOS-7) | BL-MEMOS-083..088 |
| SPR-MEMOS-09+ | not scheduled | PH-MEMOS-8 | Channel activation | BL-MEMOS-090..096 |

## 5. Backlog

Columns: **Owner**, **Depends** (items that must be done first) and **Proof** (what closes the item). "Real process" means through `apps/msp-server/bin/msp-server.mjs` via `createMspStdioCaller`, awaiting `close()` before cleanup.

### PH-MEMOS-0 — Foundations (SPR-MEMOS-00)

| ID | Epic | Title | Owner | Depends | Proof | Status |
|---|---|---|---|---|---|---|
| BL-MEMOS-001 | — | Session/episodic/instance memory design v0.1.0b → v0.2.3b | ATHER/COORD | — | MSP PR #15 merged (`9ce0d72`); design v0.2.3b erratum `2a3c039` | done |
| BL-MEMOS-002 | TASK-MEMOS-010 | Migration-runner `foreign-keys=off` mode for parent-table rebuilds | JANUS | — | MSP PR #16 merged (`869108d`) | done |
| BL-MEMOS-003 | TASK-MEMOS-010 | Refuse near-miss directive spellings; separate in-transaction prefix | JANUS | BL-MEMOS-002 | MSP PR #17 merged (`6600aa1`) | done |
| BL-MEMOS-004 | TASK-MEMOS-010 | Structural FK check on every pending migration, delegated to SQLite | JANUS | BL-MEMOS-003 | MSP PR #18 merged (`fcd17ba`) | done |
| BL-MEMOS-005 | TASK-MEMOS-010 | FK target type via `PRAGMA table_list`; directive post-exec probe; shadow non-key refusal | JANUS | BL-MEMOS-004 | MSP PR #19 merged (`ad83bc0`) | done |
| BL-MEMOS-006 | — | Memory OS phase and TASK-MEMOS-001..010 in zuri-ai roadmap | COORD | — | zuri-ai PR #380 merged (`f48b06a3`) | done |

### PH-MEMOS-1 — Decisions and contract (TASK-MEMOS-001, SPR-MEMOS-01)

| ID | Epic | Title | Owner | Depends | Proof | Status |
|---|---|---|---|---|---|---|
| BL-MEMOS-010 | TASK-MEMOS-001 | `docs/ADR-MSP-MEMORY-OS-MULTI-USER-MULTI-AGENT.md`: DEC-MEMOS-01..21, confirmed by the owner 2026-09-14; DEC-MEMOS-22..35 (PH-MEMOS-4 scoping), confirmed by the owner 2026-09-15; multi-user and multi-agent model; RKOI's rulings on four judgement calls, also confirmed by the owner 2026-09-14; cross-repo change list | ATHER | — | ADR file (current: v0.1.12b); linked from `docs/ARCHITECTURE.md` (0.2.7b) | review |
| BL-MEMOS-011 | TASK-MEMOS-001 | Design rewritten around API-011's real branch wire shapes: §6–§11 and §13 replaced; stage-1 migration `0008` and the stage-2 multi-agent migration specified; §15 suites; §18 MSP-only packets; concept mapping v0.2.3b → API-011 | ATHER | — | design file (current: v0.3.5b) with §0 review response | review |
| BL-MEMOS-012 | TASK-MEMOS-001 | RKOI review of BL-MEMOS-010 and BL-MEMOS-011 | RKOI | BL-MEMOS-010, BL-MEMOS-011 | Round 1 (commit `2f4d584`): NEEDS REVISION, 3 critical — answered in ADR v0.1.1b / design v0.3.1b. Round 2 (commit `92cb591`): NEEDS REVISION, 1 critical (wire values still did not match the shipped stage-1 code) — answered in ADR v0.1.2b / design v0.3.2b. Round 3 (commit `6d1a801`): NEEDS REVISION, 1 critical (the delivery grant's real claim set) — answered in ADR v0.1.3b / design v0.3.3b. Round 4 (commit `1c4a62f`): **APPROVED, 0 critical**, 9 warnings folded into ADR v0.1.4b / design v0.3.4b ahead of merge. Stage-1 code review round 2 (commit `445bd90`): a CRITICAL room-claim-required gap on the code (KIN fixing it), plus `DEC-MEMOS-16` and sweep/worker-grant corrections, folded into ADR v0.1.5b / design v0.3.5b | in-progress |
| BL-MEMOS-013 | TASK-MEMOS-001 | Commit ADR and design; PR; merge | COORD | BL-MEMOS-012 | PR merged with CI green | planned |
| BL-MEMOS-014 | TASK-MEMOS-001 | Owner confirmation request for DEC-MEMOS-01..16, raised with the recommendation for each | COORD/OWNER | BL-MEMOS-013 | owner's answers recorded in the ADR; not required to start PH-MEMOS-2..6; required for GATE-MEMOS-7 | done (owner confirmed DEC-MEMOS-01..16 and, separately, DEC-MEMOS-17..21 on 2026-09-14) |

### PH-MEMOS-2 — Thread memory safe for many users (TASK-MEMOS-002 stage 1, SPR-MEMOS-01/02)

| ID | Epic | Title | Owner | Depends | Proof | Status |
|---|---|---|---|---|---|---|
| BL-MEMOS-020 | TASK-MEMOS-002 | Port `codex/msp-thread-memory` onto `main` on branch `feat/memos-002-thread-memory`; fold branch 0008 and 0009 into one `migrations/0008_thread_memory.sql` | KIN | BL-MEMOS-005 | migration applies under `main`'s runner; branch integration/worker/cross tests adapted and green | in-progress |
| BL-MEMOS-021 | TASK-MEMOS-002 | Schema corrections in 0008 per design v0.3.5b §12.1 (**corrected, round two**: binding fields live directly on `threads`, pinned for the row's life — there is no separate `thread_bindings` table to restore, see `BL-MEMOS-100`'s cancellation): `threads`' own `channel_type`/`channel_account_id`/`external_room_ref_hmac`/`business_id` columns pinned by an UPDATE trigger, tenant-scoped `ACTIVE`-only unique binding via `idx_threads_active_binding`; `thread_kind`/`audience_kind`/grant `audienceKind` required equal on mint; tombstone-ready triggers and `redaction_state` on messages, summaries, receipt text and records; tenant-consistency triggers on every denormalized `tenant_id` (W3) | KIN | BL-MEMOS-020, **BL-MEMOS-012** | schema tests; trigger allows only the tombstone transition with every other column pinned | in-progress |
| BL-MEMOS-022 | TASK-MEMOS-002 | Membership rows: `membership_id`, `tenant_id`, load-bearing `left_at`, partial unique on the open row, leave-only UPDATE, no DELETE, one HUMAN per DIRECT thread for life; DEC-MEMOS-11 relink closes the thread and mints a new one (not a membership rewrite) | KIN | BL-MEMOS-021, **BL-MEMOS-012** | trigger tests; second-human insert refused; relink case | in-progress |
| BL-MEMOS-023 | TASK-MEMOS-002 | C-1 rules per design v0.3.5b §7: first HUMAN membership created by append bound to `grant.principalId` (DEC-MEMOS-12), no `participants` field on resolve; every other participant change requires `assertParticipants`, never `writePrivate` (W13 fix); AGENT/OPERATOR/UNKNOWN never readable; **assurance upgrade (corrected, round four): a `PENDING → VERIFIED` self-upgrade on a later append needs no claim under DEC-MEMOS-15's conditions (checked against both the incoming request and the stored row); every other assurance change still needs `assertParticipants` or the (unbuilt) lifecycle tool; a self-upgrade closes the old row and inserts a new one in one transaction, never an in-place `UPDATE`** | KIN | BL-MEMOS-022, **BL-MEMOS-012** | BL-MEMOS-031 cases | in-progress |
| BL-MEMOS-024 | TASK-MEMOS-002 | Protected record subject bound to the asserter's own principal via a BEFORE INSERT trigger (not a CHECK subquery — SQLite forbids it, RKOI finding 1); HUMAN-asserted ⇒ subject NOT NULL and self-bound; null-subject records visible to the asserter only; never surfaced to a different HUMAN participant of a GROUP/ROOM thread | KIN | BL-MEMOS-023, **BL-MEMOS-012** | planted-record case refused; trigger fires on insert, not a rejected CHECK | in-progress |
| BL-MEMOS-025 | TASK-MEMOS-002 | C-2: thread/participant/session/job lookups stay in `msp-core` (DEC-MEMOS-13, no separate package); pure grant verification plus `grant-scope-guard.mjs`'s `assertGrantScope(boolean)` in `msp-contracts`; decoupling assertion in `dependency-boundaries.test.mjs`, extended to scan for `.prepare(`/`.exec(`/`.pragma(` (W7) | KIN | BL-MEMOS-020 | boundary test | in-progress |
| BL-MEMOS-026 | TASK-MEMOS-002 | W1: caller `now` honoured only when `MSP_TEST_CLOCK=1` (renamed from `MSP_ALLOW_TEST_CLOCK`, W8), injected at the composition root, kept out of the client env allowlist | KIN | BL-MEMOS-020 | lease-theft case refused | in-progress |
| BL-MEMOS-027 | TASK-MEMOS-002 | W5: journal carries `principal_hmac`; no raw person id or room ref in any journal payload or error | KIN | BL-MEMOS-021 | journal scan after a full flow | in-progress |
| BL-MEMOS-028 | TASK-MEMOS-002 | W6/W7: tenant-scoped existence (no cross-tenant leak); `source_event_id` required, replay with identical content returns `deduplicated`, replay with different content returns `conflict` (RKOI finding 2) | KIN | BL-MEMOS-021 | cross-tenant resolve and reuse cases; replayed signed append; mismatched-content replay case | in-progress |
| BL-MEMOS-029 | TASK-MEMOS-002 | W10: typed errors per design v0.3.5b §14 (`validation_failed`, `not_found`, `thread_scope_denied`, `thread_audience_mismatch`, `conflict`, `identity_hmac_unconfigured`, `payload_too_large`, `record_subject_mismatch`, `compaction_lease_conflict`) — `agent_not_current` is a stage-2 code, not shipped in stage 1 | KIN | BL-MEMOS-020, **BL-MEMOS-012** | contract tests assert codes | in-progress |
| BL-MEMOS-030 | TASK-MEMOS-002 | Rename contract API-010 → API-011 (doc + schema) using the branch's actual frozen wire shapes per design v0.3.5b §12.1/§13 (flat epoch/hex grant, snake_case fields, `text`/`exchange_id`/`direction`/`kind`, not the invented camelCase/nested shapes RKOI rejected); client env allowlist gains `MSP_THREAD_SERVICE_KEY`, `MSP_IDENTITY_HMAC_KEY`; client CHANGELOG | KIN | BL-MEMOS-020, **BL-MEMOS-012** | `msp-client-env-allowlist.test.mjs`; keys never journaled or echoed; contract tests match the branch's real field names | in-progress |
| BL-MEMOS-031 | TASK-MEMOS-002 | `tests/security/thread-memory-scoping.security.mjs`: all four C-1 attacks and the planted record, W1, W5, W6, W7, cross-tenant resolve/append/sweep | KIN/GHOST | BL-MEMOS-023..028 | suite green through the real process | in-progress |
| BL-MEMOS-032 | TASK-MEMOS-002 | `migrate.test.mjs`: real graph 0001–0008 on fresh and populated databases; hard-coded migration counts updated; temp follow-up migration renumbered | KIN/JANUS | BL-MEMOS-021 | tests green | in-progress |
| BL-MEMOS-033 | TASK-MEMOS-002 | RKOI review of stage 1; fix-ups; PR; merge; must include running `tests/cross/zuri-thread-contract.test.mjs` via `npm run test:cross-zuri` with `MSP_TEST_ZURI_ROOT` set to the `origin/main` extract (BL-MEMOS-110) | RKOI/COORD | BL-MEMOS-020..032, BL-MEMOS-102, BL-MEMOS-103, BL-MEMOS-108, BL-MEMOS-109, BL-MEMOS-110, BL-MEMOS-111 | Docs review round 4 (commit `1c4a62f`): **APPROVED, 0 critical**, 9 warnings folded into design v0.3.4b/ADR v0.1.4b/this plan ahead of merge — including a new cross-room gap (`BL-MEMOS-111`) found with no backlog row at all. **Stage-1 code review round 2 (commit `445bd90`)** then found the room claim itself must be required (CRITICAL, KIN fixing it), a `channel_type`-mismatch gap (`DEC-MEMOS-16`), and the sweep/worker-grant corrections folded into design v0.3.5b/ADR v0.1.5b/this plan. A docs approval is not the same as this backlog item's own code review, which is still open; fix-ups across every round remain in progress | in-progress |
| BL-MEMOS-100 | — | ~~Restore `thread_bindings` as its own table~~ — **cancelled 2026-09-14.** Reading the shipped code (`migrations/0008_thread_memory.sql`) found binding fields (`channel_type`, `channel_account_id`, `external_room_ref_hmac`, `business_id`) already live directly on `threads`, pinned for the row's life by `trg_threads_pin_identity` — there was never a separate table to restore. Identity-key rotation is recorded as an accepted stage-1 gap instead (design v0.3.5b §6.2, §19); a future rotation packet would need its own migration to extract a binding table, not a fix to `0008`. Id burned, not reused, per the plan's own identifier rule | — | — | superseded by design v0.3.5b §6.2 | cancelled |
| BL-MEMOS-101 | TASK-MEMOS-010 | `msp_ping` reports `identity_surface: "configured"\|"unconfigured"`; one stderr startup diagnostic when `MSP_IDENTITY_HMAC_KEY` is absent (design §6.2) | KIN | BL-MEMOS-020 | `msp_ping` contract case; startup log assertion | **Moved out of PH-MEMOS-2/GATE-MEMOS-2 to PH-MEMOS-7 (release hardening)**: confirmed against the shipped code that stage 1's `msp_ping` (`apps/msp-server/src/server.mjs:94`) returns only `{ok, timestamp}` today — `identity_surface` is not shipped, so it cannot be a stage-1 gate criterion | planned |
| BL-MEMOS-102 | TASK-MEMOS-002 | Consistency-trigger gaps confirmed against the shipped `0008`, corrected from an earlier, wrong table list (that list named `thread_bindings` and `exchanges`, neither of which exists — see BL-MEMOS-100's cancellation): `thread_summary_invalidations.tenant_id` is `NOT NULL` with **no default** in the `CREATE TABLE` (0008 is unshipped, so this is an ordinary edit, not a follow-up migration), a tenant-consistency trigger using `IS NOT` (not `<>`, which is not NULL-safe), a no-update/no-delete trigger, and **the reconciliation handler must change from `INSERT OR IGNORE` to `INSERT ... ON CONFLICT(summary_id) DO NOTHING`** so a forgotten `tenant_id` is refused loudly instead of `OR IGNORE` silently absorbing it the same way it absorbs a legitimate duplicate-insert (**diagnosis corrected, round four**: the old `DEFAULT ''` actually *refused* the mismatched insert via the trigger's `<>` comparison — it never succeeded silently; the real bug only exists once the column is `NOT NULL` and the handler is left on `OR IGNORE`); a `chat_sessions` `UPDATE` trigger barring `tenant_id`/`thread_id` from changing after insert, and (subject to `BL-MEMOS-033` proving every flow — rotation, reconciliation, idle sweep — preserves it) a `UNIQUE (thread_id) WHERE status = 'OPEN'` index stating "at most one `OPEN` session per thread," never a claim about `CLOSING`, since reconciliation legitimately leaves one `CLOSING` and one `OPEN` session on a thread at once; a `thread_messages` trigger checking `session_id`/`exchange_id`/`reply_to_message_id` all belong to the same `thread_id`; a `thread_participants` INSERT-time tenant-consistency trigger (had none at all); `session_compaction_jobs` gains an INSERT-time session-belongs-to-thread-and-tenant check plus an identity-pinning UPDATE trigger; the same session-belongs-to-thread check is added to `session_summaries` and `protected_memory_records` (design v0.3.5b §12.1). **Extended (RKOI stage-1 code review round 2 on code commit `95629e2` — KIN is adding these to `0008` directly):** an `exchange_id`-leading index supporting the `thread_messages` exchange-consistency trigger's own lookup; typed error codes for the `exchange_id` and `reply_to` triggers specifically (not a generic refusal), with **no foreign-tenant existence oracle** (the trigger's own error must not let a caller distinguish "wrong tenant" from "no such exchange/message" by its wording or code); `thread_summary_invalidations` rows fully immutable (no permitted `UPDATE` shape at all, not even a state/status column, since these are audit-only); a `thread_injection_receipts` trigger checking its own `exchange_id` belongs to its own `thread_id` (the same cross-consistency shape `thread_messages` already needs, applied here too); `threads` rows are not deletable (a `no_delete` trigger, matching every other table's convention); `thread_delivery_receipts.message_id` must reference an `OUTBOUND` message only, never `INBOUND` | KIN | BL-MEMOS-021 | mismatched-tenant insert refused on `thread_summary_invalidations` and `thread_participants`; an INSERT to `thread_summary_invalidations` omitting `tenant_id` is refused (the tenant-consistency trigger fires before the NOT NULL check, so assert refusal, not a specific NOT NULL message) and is not swallowed by `ON CONFLICT(summary_id) DO NOTHING`; post-insert `UPDATE` of `chat_sessions`/`session_compaction_jobs` identity columns refused; a second `OPEN` session on the same thread is refused (or, if left code-enforced pending `BL-MEMOS-033`'s proof, at least tested through the real process) while a `CLOSING` + `OPEN` pair on one thread is accepted; cross-thread `session_id`/`exchange_id`/`reply_to_message_id` insert refused on `thread_messages`, `session_summaries` and `protected_memory_records`, each with its own typed error and no tenant-existence signal leaked; a `session_compaction_jobs` row naming another thread's or tenant's session is refused; a `thread_summary_invalidations` row can never be updated by any statement shape; a `thread_injection_receipts` row naming an `exchange_id` of a different thread is refused; `DELETE FROM threads` is refused; a `thread_delivery_receipts` row naming an `INBOUND` message is refused; **a delivery reconciled after its session has already closed still succeeds, and the resulting invalidation row for that summary can be queried directly from the database** — not merely that the call itself succeeds (`tests/security/thread-memory-scoping.security.mjs`, design §15) | planned |
| BL-MEMOS-103 | TASK-MEMOS-002 | `dependency-boundaries.test.mjs` source-scans `packages/msp-contracts/src` for `.prepare(`, `.exec(`, `.pragma(` (W7, the structural C-2 proof) — **already shipped**, confirmed against `thread-access.mjs`'s own header comment describing this exact scan | GHOST | BL-MEMOS-025 | test fails if any of the three substrings appears | done |
| BL-MEMOS-108 | TASK-MEMOS-002 | An `UPDATE`-pinning trigger for `thread_injection_receipts`'s state machine (design v0.3.5b §9.3, §12.1) — confirmed against the shipped code that this transition is enforced in JS only (`thread-memory.mjs:1074-1092`), with no database trigger backstop, unlike every other content table's tombstone/status trigger. **The trigger must also pin `injection_id` itself**: a primary-key-only rewrite (state/version untouched) was accepted by a draft trigger that pinned every other column but not the key | KIN | BL-MEMOS-021 | a direct `UPDATE` bypassing the handler (e.g. `RESOLVED → COMPLETED` skipping `SUBMITTED`) is refused at the database layer; a bare `UPDATE ... SET injection_id = ?` with state/version unchanged is refused (`tests/security/thread-memory-scoping.security.mjs`) | planned |
| BL-MEMOS-109 | TASK-MEMOS-002 | **Widened again (RKOI round four, correcting round three's own "check when present" framing)** — zuri-ai's real delivery grant is exactly `{tenantId, businessId, channelAccountId, externalRoomRef, principalId, policyRevision, deliveryWriter}` — no `audienceKind`, no `channelType`, and the other five thread tools' grants always carry `audienceKind`. Fixes to `thread-guard.mjs`: (1) require `audienceKind` on `resolve`/`append`/`context`/`memory_record`/`injection_record`, refusing it when absent — not merely "check when present"; (2) `msp_thread_delivery_record` is the one named exemption from that requirement, scoped instead by the inbound message's own thread plus the room hash; if a delivery grant ever does carry `audienceKind` anyway it is still checked; (3) stop requiring a `channelType` claim on any grant, and re-derive the delivery scope check from exactly `tenantId`/`businessId`/`channelAccountId`/`externalRoomRef` (design v0.3.5b §6.1, §9.2, §13); (4) update `docs/API-011-THREAD-MEMORY-CONTRACT.md:54,196` and the `test:cross-zuri` harness's own header comment, both of which still describe the four-segment room hash | KIN | BL-MEMOS-020 | Acceptance test uses zuri's exact delivery claims (no `audienceKind`, no `channelType`) on **both** paths: a delivery record for a thread whose inbound message already exists succeeds, and a delivery record whose inbound message has not been seen yet stays correctly `pending`; missing `audienceKind` on any of the other five tools is refused. Also passes `npm run test:cross-zuri` with `MSP_TEST_ZURI_ROOT` set (BL-MEMOS-110) | planned |
| BL-MEMOS-110 | TASK-MEMOS-002 | **Reworded (RKOI round four): the script and the test already exist.** Make `npm run test:cross-zuri` (`tests/cross/zuri-thread-contract.test.mjs`) pass against a read-only extract of zuri-ai `origin/main` via `MSP_TEST_ZURI_ROOT`, and wire it into `GATE-MEMOS-2` — this item is about making it pass and gating on it, not building it from nothing | JANUS/GHOST | BL-MEMOS-020 | `npm run test:cross-zuri` passes in CI and locally against a real `MSP_TEST_ZURI_ROOT` extract; `GATE-MEMOS-2` names it explicitly | planned |
| BL-MEMOS-111 | TASK-MEMOS-002 | **New (RKOI round four): no backlog row previously covered this at all.** A call against an existing thread checks tenant, business and channel account, but not the room; `msp_session_compaction_claim` resolves its thread via `job_id` with no scope check of any kind. Fix: recompute the grant's own room hash (`tenant_id\|channel_account_id\|external_room_ref` under `MSP_IDENTITY_HMAC_KEY`) and compare it against the resolved thread's stored `external_room_ref_hmac` on **every** thread-bound call, including `claim`/`commit`/`retry` via the job's own thread — not only `resolve` and not only the tools that take a `thread_id`/`session_id` directly (design §6.3, §12.1, §15). **Widened (RKOI stage-1 code review round 2 on code commit `95629e2`) — CRITICAL on the code, KIN fixing it: the room claim itself must be required, not merely compared when present.** The shipped guard's check is gated `if (grant.externalRoomRef)`, so a grant with no room claim at all currently skips the comparison and passes on tenant/business/account alone. Fix: any thread-bound call whose grant lacks `externalRoomRef` or `channelAccountId` is refused `thread_scope_denied` outright — `context`, `append`, `memory_record`, `injection_record`, `delivery_record`, `claim`, `commit`, `retry`, and `sweep` | KIN | BL-MEMOS-021 | a grant scoped to room R1 is refused against a thread that only shares R1's tenant/business/channel-account, including via `claim`/`commit`/`retry`; a `claim` response never leaks a different room's `sources`; a grant with no room claim at all is refused on every one of the eight thread-bound tools listed above, not merely mismatched-but-refused (`tests/security/thread-memory-scoping.security.mjs`) | planned |

### PH-MEMOS-3 — Multi-agent (TASK-MEMOS-002 stage 2, SPR-MEMOS-03)

**Fully scoped against design v0.4.1b, not a sketch** (§6.1.1, §8, §9.4,
§12.2, §13, §14, §15) — **revised once already**: RKOI's stage-2 review
round 1 (commit `f74ad0d`) returned NEEDS REVISION, 2 critical, answered
in this revision. **Built stacked on the unmerged stage-1 branch**
(`feat/memos-002-thread-memory`) — every row below additionally depends
on `BL-MEMOS-013` (stage-1 docs merged) and `BL-MEMOS-033` (stage-1 code
review closed and merged to `main`), since stage 2's migration and grant
checks extend stage-1's own migration and grant checks; merge of this
phase's own PR waits for both.

| ID | Epic | Title | Owner | Depends | Proof | Status |
|---|---|---|---|---|---|---|
| BL-MEMOS-040 | TASK-MEMOS-002 | Extend `verifyThreadGrant`'s required-claim check to also require `agentId`/`workspaceId` on every one of the ten API-011 tools, reusing `grant_signature_invalid` (not a new code); journal `actor = grant.agentId` in plain text for agent-attributable entries, replacing the `workspaceId: job.tenant_id` placeholder already shipped in `thread-memory.mjs:931,1037` (design §6.1.1, §8.4) | KIN | BL-MEMOS-013, BL-MEMOS-033 | grant tests assert `grant_signature_invalid` on a missing `agentId`/`workspaceId` on all ten tools; journal scan shows `actor` as the raw `agentId`, never HMAC'd, and never a raw person id (not a W5 regression) | planned |
| BL-MEMOS-041 | TASK-MEMOS-002 | `thread_agents (agent_attachment_id, tenant_id, thread_id, agent_id, workspace_id, joined_at, left_at)`: append-only, partial unique on the open row, tenant-consistency trigger, no DELETE — the structural analogue of `thread_participants` for agents (design §8.1). Attached only two ways: the calling agent's own resolve mints the thread (`created: true`, but only after that call's own thread `INSERT` wins the mint race — never merely because no row was found first), or its own grant carries `assertAgents === true` against an *existing* thread — `assertAgents` is read only on `msp_thread_resolve`, never any other tool. A worker-only grant (`operator`, no reader/writer flags) never mints — `not_found` if the room has no `ACTIVE` thread (`DEC-MEMOS-18`) | KIN | BL-MEMOS-040 | schema and trigger tests (design v0.4.1b §12.2, stage-2 migration `00NN_thread_agents.sql`, KIN names it `0009` on the branch unless something else merges first, per DEC-MEMOS-14); append-only/no-delete/tenant-consistency cases mirroring `thread_participants`' own suite; mint-race case (two concurrent resolves for one room, exactly one auto-attach); worker-only-grant-never-mints case | planned |
| BL-MEMOS-042 | TASK-MEMOS-002 | Agent gate, per tool exactly as design §8.2 tables it: `resolve` (existing thread, current-or-`assertAgents`), `append` (current, plus `speaker_id === grant.agentId` for an `AGENT`-kind message), `context`/`memory_record`/`injection_record` (current, layered on top of every existing HUMAN/participant check, never replacing it), `delivery_record` (**both paths, corrected CRITICAL 1**: resolved path via the inbound message's own thread; pending path via `ThreadRegistry#findThreadByRoom` against the room's `ACTIVE` thread, `not_found` if none), `claim`/`commit`/`retry` (current on the job's resolved thread, `DEC-MEMOS-18`), `sweep` (exempt from "current," `agentId`/`workspaceId` still required present, `DEC-MEMOS-18`) — a non-current agent anywhere this applies gets the new `agent_not_current`/`AgentNotCurrentError`. **New (RKOI stage-2 review round 2, finding 6, ruling): `sweep`'s per-job metadata gains `thread_kind` and `channel_type`** — non-sensitive, room-scoped, still no `sources` — so the worker can construct its own subsequent `msp_thread_resolve` attach call for that job's room, whose required fields include both; the worker's own resolve grant sources `audienceKind` from its Tier-1 room configuration, the same source as its room claims, not from `sweep`'s response | KIN | BL-MEMOS-041 | BL-MEMOS-045 cases, one per tool row in design §8.2/§15, including both delivery paths; a `sweep` response's job metadata includes `thread_kind`/`channel_type` for every job | planned |
| BL-MEMOS-043 | TASK-MEMOS-002 | `protected_memory_records` gains `agent_id TEXT` (nullable — legacy rows stay `NULL`, required by the handler once `agentId` is a required grant claim) and `visibility TEXT NOT NULL DEFAULT 'THREAD' CHECK (visibility IN ('AGENT','THREAD'))` via `ALTER TABLE`, no rebuild; `trg_protected_memory_records_update_guard` dropped and recreated to pin both new columns in its existing two permitted UPDATE shapes; new `trg_protected_memory_records_agent_rules` trigger refuses `visibility='AGENT'` with a `NULL agent_id`, and refuses an `agent_id` that never attached to the record's own thread (a `CHECK` cannot express either rule — SQLite has no `ADD CONSTRAINT`) (design §9.4, §12.2 — SQLite triggers cannot be altered in place); new optional `visibility` request field on `msp_thread_memory_record`, default `THREAD` (`DEC-MEMOS-19`). **CRITICAL 2, corrected, then unified (RKOI stage-2 review round 2, `72e593f`, owner-direction ruling)**: `record_id`'s content hash gains `agent_id`/`visibility` as inputs, so two agents recording identical content get two distinct records. **Supersession answers are unified to one identical `validation_failed`** with the fixed message "supersedes_record_id does not name a record this caller can supersede" for all three cases — unknown id, another agent's `AGENT`-visibility record, and a record failing the pre-existing stage-1 ownership/status check (which was `conflict` in stage 1, now also `validation_failed` in stage 2) — checked in that order (existence/cross-agent first, then ownership/status), never `thread_scope_denied` (round 1's own fix was itself a third oracle value) and never three distinguishable codes. `THREAD`-visibility and legacy `agent_id IS NULL` records stay supersedable by any agent under the stage-1 rules alone Stage 1's race-time supersession `conflict` (`thread-memory.mjs:834`) also returns the same `validation_failed` answer; `AGENT` ownership is by `agent_id` alone. | KIN | BL-MEMOS-041 | visibility cases: an `AGENT`-visibility record is invisible to a different current agent; a `THREAD`-visibility record and every legacy (`agent_id IS NULL`) row are visible to any current agent; an attempted `UPDATE` of `agent_id`/`visibility` post-insert is refused by the recreated guard trigger; a record with `visibility='AGENT'` and no `agent_id` is refused at insert; an `agent_id` naming an agent that never attached is refused at insert; agent B recording identical content to agent A's `AGENT`-visibility record gets its own distinct `record_id`; agent B superseding agent A's `AGENT`-visibility record, an unknown id, and a stage-1 ownership/status failure all produce byte-identical `validation_failed` responses; a `THREAD`-visibility or legacy record remains supersedable by any agent under stage-1 rules | planned |
| BL-MEMOS-112 | TASK-MEMOS-002 | **New (RKOI stage-2 review round 1, CRITICAL 1, commit `f74ad0d`), corrected (RKOI stage-2 review round 2, `72e593f`, findings 2–4).** `thread_pending_deliveries` gains `agent_id TEXT`/`workspace_id TEXT` (nullable — a pre-stage-2 pending row in flight at cutover has neither, but the stage-2 handler requires both on every new insert); new `ThreadRegistry#findThreadByRoom(tenantId, channelAccountId, externalRoomRefHmac)` method, promoted from `resolveThread`'s own inline binding lookup (`thread-memory.mjs:474,518`); the pending-insert path requires the calling agent be current on the room's `ACTIVE` thread found via that method, `not_found` if none exists; the previously-unwrapped pending insert (`thread-memory.mjs:1115-1116`) is wrapped in its own transaction, alongside the nonce insert (`BL-MEMOS-048`). **`trg_thread_pending_deliveries_update_guard` is dropped and recreated (finding 2) to also pin `agent_id`/`workspace_id` in both its permitted UPDATE shapes** — the migration's own comment correcting that `trg_protected_memory_records_update_guard` was NOT the only drop+recreate. **At drain time (`#drainDeliveries`), the stored `(agent_id, workspace_id)` is re-checked for currency on the inbound message's own thread (not a freshly re-derived room `ACTIVE` thread, finding 4)**; a departed agent, or a legacy row with a `NULL` stored agent (which fails closed by construction, never drained), leaves the row unreconciled with the existing `reconcile_skipped` journal shape (`error_code: 'agent_not_current'`, reusing the existing stable-code catch, `thread-memory.mjs:1196`). **Speaker id, both paths (finding 3, no hard-coded label anywhere)**: the resolved path's internal append uses `speakerId: grant.agentId`; the drain path's uses the stored `agent_id` | KIN | BL-MEMOS-041, BL-MEMOS-048 | a delivery-writer not current on the room's `ACTIVE` thread cannot queue a pending delivery for it; a room with no `ACTIVE` thread refuses the pending insert `not_found`; a reconcile `UPDATE` that changes the stored `agent_id`/`workspace_id` is refused by the recreated guard trigger; an agent current at queue time but departed by drain time, checked against the inbound message's own thread, leaves the row `reconcile_state='pending'` with a `reconcile_skipped` journal entry; a legacy `NULL`-agent pending row is never drained; a resolved-path delivery's internal message has `speaker_id === grant.agentId`; a drained message's has `speaker_id` equal to the stored `agent_id` | planned |
| BL-MEMOS-044 | TASK-MEMOS-002 | Summaries need no new column (design §9.4): `msp_thread_context`'s agent gate (`BL-MEMOS-042`) alone already fully gates a departed agent's access to every summary on the thread — no per-summary filter to build | KIN | BL-MEMOS-042 | departure case: a departed agent's very next `msp_thread_context` call is refused `agent_not_current` before any summary is read, not merely a filtered response | planned |
| BL-MEMOS-045 | TASK-MEMOS-002 | `tests/security/thread-agent-scoping.security.mjs`: agent B vs agent A `AGENT`-visibility records, including dedup (B's identical content gets its own `record_id`) and supersession (B superseding A's record is `thread_scope_denied`, not an oracle); a departed agent denied on every thread-bound tool, not merely `context` (revocation is a per-call property, not a control, `DEC-MEMOS-18`); an unattached agent (no `thread_agents` row at all) refused identically to a departed one; cross-tenant agent; mint-race (two concurrent resolves, exactly one auto-attach); worker-only grant never mints; compaction lease across agents (`DEC-MEMOS-18`'s agent-currency-on-the-job's-thread rule); delivery's pending-path agent check and drain-time re-check (`BL-MEMOS-112`); nonce replay (`grant_replayed`) and missing-nonce (`grant_nonce_required`) on every nonce-required tool, transaction-rollback confirmed; legacy-record visibility (every pre-stage-2 row reads as `THREAD`); `requesterAgentId` absent sees `THREAD` only. **Keyring cases live in their own suite, `tests/security/thread-service-keyring.security.mjs` (`BL-MEMOS-049`), not here** | GHOST | BL-MEMOS-042..044, BL-MEMOS-048, BL-MEMOS-112 | suite green through the real process, one case per design §15 stage-2 row | planned |
| BL-MEMOS-046 | TASK-MEMOS-002 | API-011 contract and schema update for `agentId`/`workspaceId`/`nonce`/`assertAgents` and the `thread_agents`/`grant_nonces` shapes; record the zuri-ai follow-up (`BL-MEMOS-107`: grant must add `agentId`/`workspaceId`/`nonce` [≥128 random bits, `DEC-MEMOS-20`]/`assertAgents`, plus the outbound-append `agentId === speakerId` requirement, one PR) against `RSK-MEMOS-01`, including `DEC-MEMOS-17`'s "no compatibility flag" note directly on that item | KIN/ATHER | BL-MEMOS-040 | contract doc and schema updated; `RSK-MEMOS-01` updated with the no-compatibility-flag note | planned |
| BL-MEMOS-047 | TASK-MEMOS-002 | RKOI review of stage 2; PR; merge. `test:cross-zuri` (also `GATE-MEMOS-2`'s own suite) gains the `DEC-MEMOS-17` cases, **worded to name the deliberate flip**: before `BL-MEMOS-107` lands, it asserts zuri-ai's `origin/main` grant is refused with a typed grant error; after `BL-MEMOS-107` lands, the same suite must pass unmodified. A separate "wrapped port" case, **labelled as proving only that MSP accepts the shape**, not that zuri-ai has adopted it, is not part of the pass/fail flip | RKOI/COORD | BL-MEMOS-040..046, BL-MEMOS-048, BL-MEMOS-049, BL-MEMOS-112 | APPROVED 0 critical; merged; both `test:cross-zuri` cases pass, each labelled per its own claim | planned |
| BL-MEMOS-048 | TASK-MEMOS-002 | Grant replay protection: `nonce` required on every mutating tool except `msp_thread_message_append` (which keeps `source_event_id` idempotency) and except `msp_thread_context` (read-only, nothing to replay) — `resolve`, `memory_record`, `injection_record`, `delivery_record`, `sweep`, `claim`, `commit`, `retry`. Missing the claim on a nonce-required tool is `grant_nonce_required`; a reused, unexpired `(tenantId, nonce)` is `grant_replayed`. **Corrected (RKOI stage-2 review round 1, warning 2): the nonce `INSERT` does not run in the guard.** `thread-guard.mjs`'s handlers are `async`; better-sqlite3 refuses an async transaction callback. The verified `nonce`/`tenantId` are instead passed down as parameters into whichever `msp-core` store method the handler calls, and that method's own existing synchronous `this.#db.transaction()` body performs the nonce insert alongside its own mutation, so a replay never partially applies. Nonce length/format per `DEC-MEMOS-20` (≥128 random bits, ≤128 chars). **Corrected (RKOI stage-2 review round 2, finding 5): `resolve`'s nonce is consumed on every one of its outcomes** — mint, self-assert attach (`assertAgents` inserting a `thread_agents` row), and the no-op case (already-current agent, nothing changes) — all inside `resolve`'s own transaction; the nonce insert rolls back only when the call itself is refused end-to-end, never merely because the outcome was a no-op | KIN | BL-MEMOS-040 | replayed-grant and missing-nonce cases for every nonce-required tool; an append and a context call need neither and are unaffected; a replayed call's mutation is confirmed absent (transaction rolled back), not merely refused at the API layer; a code-review check that no nonce insert lives inside `thread-guard.mjs` itself; a `resolve` call whose outcome is a no-op (already-current agent, existing thread) still consumes its nonce, confirmed by a same-nonce retry on an otherwise-identical no-op resolve being refused `grant_replayed` | planned |
| BL-MEMOS-049 | TASK-MEMOS-002 | **RKOI-approved at `fd8095f` — `MSP_THREAD_SERVICE_KEYRING` is JSON-object-only, no file-path form.** Malformed configuration refuses the server to start with new code `thread_keyring_config_invalid`, parsed **before the database opens**; refused at start: malformed JSON, an empty `{}`, a duplicate tenant id (including escaped equivalents), a tenant id differing from its own trimmed form (including non-ASCII whitespace and an escaped tab), a whitespace-only or padded or under-32-character key value — each error names only the failing entry's 1-based position, never an id, a key, or a `cause`; an empty env value is treated as unset. The parsed keyring map has no prototype (`Object.create(null)`) and lookup is own-property only — **RKOI's final ruling: `__proto__`/`constructor`/`toString` and similar are accepted and honoured as ordinary tenant ids, not refused** — refusing them only in keyring mode would reject tenants single-key mode already serves, for no matching security benefit once the map itself has no prototype to shadow. When configured (and valid), **replaces** the single default key for every tenant with no fallback — a tenant absent from a valid keyring is `grant_unconfigured` at grant-verification time. Selected by the grant's own unverified `tenantId`, then that same key must make the signature verify (design §6.1.1). **New risk `RSK-MEMOS-11`**: whether a tenant is in the keyring is visible from `grant_unconfigured` vs `grant_signature_invalid` — accepted, stdio trust boundary. **New open owner question, not a decision (§19)**: whether to restrict tenant ids to a safe character set in both modes, rejecting zero-width/control-character ids accepted today; default is no restriction, not a merge blocker. **Test evidence**: `tests/security/thread-service-keyring.security.mjs` (9 cases) plus `tests/contract/thread-service-keyring.test.mjs`, not `thread-agent-scoping.security.mjs` | KIN | BL-MEMOS-040 | startup refusal for each malformed case above, naming only the entry position; a valid keyring's cross-tenant forged-grant case refused; single-key default unchanged when no keyring is set; no fallback to the default once a keyring is set; a tenant literally named `__proto__` is stored and read back as an ordinary id and its configured key verifies correctly | in-progress |
| BL-MEMOS-104 | TASK-MEMOS-002 | Opportunistic `grant_nonces` pruning on insert, bounded and never dependent on a retention tick (RKOI ruling 3): `DELETE FROM grant_nonces WHERE rowid IN (SELECT rowid FROM grant_nonces WHERE expires_at < ? LIMIT 200)`, run in the same transaction immediately before the nonce insert — the `LIMIT` lives inside the subquery so this needs no `SQLITE_ENABLE_UPDATE_DELETE_LIMIT` build flag. **Corrected (RKOI stage-2 review round 1, warning 2): the bound is always the server's own wall clock (`Date.now()`), never a caller-supplied `now` under `MSP_TEST_CLOCK`** (design §12.2) | KIN | BL-MEMOS-048 | expired-row prune observed on a subsequent insert without a retention tick call; prune cost bounded to 200 rows regardless of table size; a test asserting `MSP_TEST_CLOCK`'s `now` has no effect on which rows are pruned | planned |

### PH-MEMOS-4 — Lifecycle, erasure, retention (TASK-MEMOS-003, TASK-MEMOS-004, SPR-MEMOS-04)

| ID | Epic | Title | Owner | Depends | Proof | Status |
|---|---|---|---|---|---|---|
| BL-MEMOS-050 | TASK-MEMOS-003 | **Fully scoped, design v0.5.3b §7.1.** `msp_thread_participant_lifecycle`: `leave` (self or third-party `speaker_id`, `assertParticipants` required unconditionally, `DEC-MEMOS-22`, only ever sets `left_at` — never closes the thread) and `close_for_relink` (`DIRECT`-only, `assertParticipants` **plus** the new `assertRelink` claim, `DEC-MEMOS-23`; closes the current HUMAN row and `threads.status → 'CLOSED'` in one transaction, reusing the already-shipped `0008` triggers, no schema change; **new (WARNING 2, `close_for_relink` status race): the `threads` UPDATE is `WHERE thread_id=? AND status='ACTIVE'`, checking the affected-row count and refusing a typed error if zero, and the append/participant-insert code paths re-check `status='ACTIVE'` inside their own synchronous store transaction, not only at the guard layer**); a new principal never inherits the closed thread's history (DEC-MEMOS-11 unchanged) | KIN | BL-MEMOS-047 | relink case; leave case (self and third-party); missing-`assertRelink` case; close-then-write race case (guard passes, store transaction's own re-check refuses) | planned |
| BL-MEMOS-051 | TASK-MEMOS-003 | **Fully scoped, design v0.5.0b §8.6.** `msp_thread_agent_detach`: closes the calling agent's own `thread_agents` row (`left_at`); no mechanism to detach a *different* agent's identity, mirroring design §8.1's "attachment happens exactly two ways, never a third" (there is likewise no third-party detach); needs no new grant claim (`DEC-MEMOS-24`) — the existing generic agent-currency gate already guarantees the row exists before the handler runs | KIN | BL-MEMOS-047 | detach case; `agent_not_current` on the very next call from the detached agent | planned |
| BL-MEMOS-052 | TASK-MEMOS-003 | Lifecycle security cases: relinked principal denied on the old (now-closed) DIRECT thread, including pre-departure history, and the new thread carries none of the old one's data; `leave` without `assertParticipants` refused, self or third-party; **the full three-way branch (§7 rule 2), corrected round 3 to name all five required cases explicitly**: never-joined append succeeds with no claim (case 1, regression); a departed principal's next plain `HUMAN` append is refused `thread_scope_denied`, not a silent rejoin (case 2, the bug, `CRITICAL 1`, `BL-MEMOS-058`); departed rejoin **with** `assertParticipants` succeeds, creating a new row (case 2, the legitimate path); a current row's `DEC-MEMOS-15` self-upgrade with no claim still succeeds (case 3b, regression-proofing `DEC-MEMOS-15` against this same fix); **and (case 2b, new, RKOI PH-MEMOS-4 review round 3, WARNING 1) a Tier-1 caller with `assertParticipants` can re-attach a *different, departed third party* to a `GROUP`/`ROOM` thread — an intended consequence of the three-way branch, not a gap — refused unconditionally on `DIRECT` threads by the existing single-HUMAN schema constraint**; detached agent denied; `close_for_relink`'s status-race case (BL-MEMOS-050), **including the assertion that the raw `SqliteError` code (narrowed to exactly `SQLITE_BUSY_SNAPSHOT`, RKOI PH-MEMOS-4 review round 3, WARNING 2) never reaches the caller, only the typed `conflict`** | GHOST | BL-MEMOS-050, BL-MEMOS-051, BL-MEMOS-058 | `tests/security/participant-lifecycle-relink.security.mjs` — one suite file for every relink case, not split across `thread-memory-scoping`/`thread-agent-scoping` (design §15, RKOI review) | planned |
| BL-MEMOS-053 | TASK-MEMOS-004 | **Fully scoped, design v0.5.3b §11.2.** `msp_thread_principal_erase` (`DEC-MEMOS-26`): not thread-bound, tenant/principal-scoped; requires `dataSubjectAccess` (self) plus `dataSubjectAdmin` for another principal (`DEC-MEMOS-25`); idempotent by `(tenant_id, idempotency_key)`, 1–128 chars (`DEC-MEMOS-27`) — same-principal replay returns the stored receipt with no writes, different-principal replay is `conflict`; tombstones authored messages (own `speaker_id`) and records (asserter-or-subject, blanking **both** `body_json` and `scope_json` — requires migration `0010`, `CRITICAL 4` item 1) via the existing `0008`/`0009` tombstone triggers plus `0010`'s recreated one, no new trigger beyond that; summaries and `thread_delivery_receipts` via `DEC-MEMOS-34`'s sole-ever-HUMAN-participant test — two independent conditions ANDed, corrected round 2 (`CRITICAL 2`, WARNING 4): the participant-history count (either `speaker_id` or non-null `person_id` exceeding 1 disqualifies) **and** no `thread_messages` row with `speaker_kind NOT IN ('HUMAN', 'AGENT')` anywhere on the thread; **`thread_pending_deliveries` removed from scope entirely — asserted untouched (`CRITICAL 4` item 2, no reliable principal-attribution column, holds AGENT-authored reply text)**; **transaction order is resolve-then-tombstone-then-close-participants-last** — kept as defense in depth for a future `left_at`-filtered query, not because today's `DEC-MEMOS-34` query (no `left_at` filter) actually depends on it (corrected round 2, WARNING 1); closes open `thread_participants` rows last; `erasure_receipts` (new table, §12.3, raw `principal_id`, `DEC-MEMOS-28`); journal pseudonym only (`principalHmac`, counts/idempotency-key payload, no raw id, no content); an unknown principal is a zero-count success, never `not_found` (`DEC-MEMOS-33`) | KIN | BL-MEMOS-050, BL-MEMOS-059 | direct-database assertions after `close()`, including `scope_json` blanked and `thread_pending_deliveries` unchanged; tools blind; idempotent-replay case (no further writes); different-principal-same-key `conflict` case; GROUP-thread non-sole-HUMAN case leaves summaries/receipts untouched; **UNKNOWN-speaker case leaves summaries/receipts untouched even with a sole-ever HUMAN participant (`CRITICAL 2`)**; **`speaker_id`/`person_id` two-count case (WARNING 4)**; **ordering property test (WARNING 2, corrected from the withdrawn false "closing first would break the result" claim): both orderings of the transaction produce an identical result** | planned |
| BL-MEMOS-054 | TASK-MEMOS-004 | **Fully scoped, design v0.5.3b §11.2.** `msp_thread_retention_tick` (`DEC-MEMOS-26`): operator-bound to its own tenant via an explicit guard name check (not the `msp_session_` prefix match), **deliberately whole-tenant not room-scoped even under a room-claimed operator grant — a stated ruling, not a gap**; `dry_run` supported (candidate counts, no mutation, **consumes no nonce — `DEC-MEMOS-35`**; **revised round 2, WARNING 6: `dry_run: true` now also writes a journal entry, like `dry_run: false` does — only the nonce exemption is dry-run-specific**); age-based on a single deployment-wide `MSP_THREAD_RETENTION_DAYS` env var, principal-agnostic — no per-tenant policy table this phase (`DEC-MEMOS-29`, partial `RSK-MEMOS-06` mitigation); reuses erasure's own tombstone mechanism (including `thread_pending_deliveries`, which stays in retention's scope even though it left erasure's), bounded to 200 rows/table/call (reusing `DEC-MEMOS-20`'s bound); never touches `thread_participants`/`thread_agents`/`threads`/`grant_nonces` | KIN | BL-MEMOS-053 | cross-tenant operator case refused (by construction — every row touched is `WHERE tenant_id = grant.tenantId`); whole-tenant-not-room-scoped case, proving a room-claimed operator grant still sweeps the whole tenant; **`dry_run` assertion, corrected round 2 (WARNING 2) — row counts cannot distinguish dry from live (both use `UPDATE`, never `DELETE`)**: `COUNT(*) WHERE redaction_state != 'none'` is `0` after `dry_run: true` and non-zero after `dry_run: false`; content columns unchanged after dry, blanked after live; `dry_run: true` consumes no nonce but **does** write a journal entry, confirmed on both arms | planned |
| BL-MEMOS-055 | TASK-MEMOS-004 | **Fully scoped, design v0.5.3b §11.2.** `msp_thread_principal_export`: own messages, own records, `session_summaries` only for threads where the principal was the thread's sole-ever `HUMAN` participant with no disqualifying `UNKNOWN`/`OPERATOR` message (`DEC-MEMOS-34`, corrected from a thread-level "current or departed participant" test that leaked GROUP-thread content, `CRITICAL 3`; gains the second, independent message-level condition round 2, `CRITICAL 2`); self or `dataSubjectAdmin` within the tenant (same flag pair as erasure, `DEC-MEMOS-25`, not a separate role); excludes every tombstoned row including the exporter's own previously-erased content (`DEC-MEMOS-30`); ignores agent `visibility` entirely — an `AGENT`-visibility record is included for its own asserter/subject regardless of which agent recorded it (`DEC-MEMOS-31`); an unknown principal is an empty export, never `not_found` (`DEC-MEMOS-33`) | KIN | BL-MEMOS-053 | export contains only the principal's own material; excludes tombstoned rows including the exporter's own; GROUP-thread non-sole-HUMAN case returns no summary from that thread at all; **UNKNOWN-speaker case returns no summary either, even with a sole-ever HUMAN participant (`CRITICAL 2`)**; cross-principal export without `dataSubjectAdmin` refused | planned |
| BL-MEMOS-056 | TASK-MEMOS-004 | `tests/security/thread-erasure.security.mjs`: direct-table and tool assertions, asserting the full erasure result (row counts and content, not merely call success); idempotency (same-key/same-principal no-op, same-key/different-principal `conflict`); **GROUP-thread `DEC-MEMOS-34` reproduction, both erasure and export directions (`p1` erases/exports, `p2` authored the summarized content) — required cases**; **UNKNOWN-speaker `DEC-MEMOS-34` reproduction, both directions, required (round 2, `CRITICAL 2`): a GROUP thread, sole HUMAN participant, one `UNKNOWN`-speaker message — excluded from both erasure and export**; **`speaker_id`/`person_id` two-count disqualification case, required (round 2, WARNING 4)**; `scope_json` blanked on erasure (`CRITICAL 4` item 1); `thread_pending_deliveries` untouched by erasure; **transaction-ordering property test (round 2, WARNING 1, corrected from the withdrawn false "closing first would break the result" claim): both orderings produce an identical result**; export and erase cannot name another principal without `dataSubjectAdmin` (`DEC-MEMOS-25`); **retention `dry_run` (round 2, WARNING 2, corrected: `redaction_state`-count and content-column assertion, not vacuous row counts; no nonce, but a journal entry IS written on both arms, round 2 WARNING 6) and cross-tenant/whole-tenant cases** | GHOST | BL-MEMOS-053..055 | suite green | planned |
| BL-MEMOS-057 | TASK-MEMOS-003/004 | RKOI review; PR; merge | RKOI/COORD | BL-MEMOS-050..056, BL-MEMOS-058, BL-MEMOS-059 | APPROVED 0 critical; merged | planned |
| BL-MEMOS-058 | TASK-MEMOS-003 | **New (RKOI PH-MEMOS-4 review, CRITICAL 1). Re-specified round 2 (still open — round 1's fix was under-specified and both literal readings of it failed).** Fix `thread-guard.mjs`'s `msp_thread_message_append` participant branch as an explicit **three-way branch**, not a modified condition on the existing two: (1) `!hasEverParticipated` — unchanged fast path, no claim; (2) `hasEverParticipated && findCurrentParticipant === null` — a rejoin, **unconditionally** requires `assertParticipants`, **never** reads `current.personId` or any field of `current` (which is `null` by this case's own definition), **never** falls through to `DEC-MEMOS-15`'s self-upgrade exception; (3) `findCurrentParticipant !== null` — today's existing logic, unchanged (third-party requires `assertParticipants`; self reaches `DEC-MEMOS-15`, reading `current.personId` safely since `current` is non-null here by construction). New `ThreadRegistry#hasEverParticipated(threadId, speakerId)` (`SELECT 1 FROM thread_participants WHERE thread_id = ? AND speaker_id = ? LIMIT 1`, no `left_at` filter). Corrects already-shipped `main` code (`thread-guard.mjs:279-320`), not new-tool scope | KIN | — | four required cases: never-joined append succeeds with no claim (case 1); departed rejoin with no claim is refused `thread_scope_denied` (case 2, the bug); departed rejoin with `assertParticipants` succeeds, creating a new row (case 2, the legitimate path); a current row's `DEC-MEMOS-15` self-upgrade with no claim still succeeds (case 3, regression-proofing `DEC-MEMOS-15` against this same fix) | planned |
| BL-MEMOS-059 | TASK-MEMOS-004 | **New (RKOI PH-MEMOS-4 review, CRITICAL 4 item 1).** New migration `migrations/0010_erasure_receipts.sql` (provisional name, `DEC-MEMOS-14`/`07`'s merge-order rule — KIN renumbers if something merges first): drops and recreates `trg_protected_memory_records_update_guard` so its tombstone branch also permits `scope_json → '{}'` alongside `body_json → '{}'`; additive trigger replacement, no table rebuild, no rootpage change, same drop+recreate pattern `0009` already used twice on this table's and `thread_pending_deliveries`' own guards; carries the `erasure_receipts` table (design §12.3) in the same file. **Requires `0009`, not merely `0008` (round 2, WARNING 7): the recreated trigger's body references `agent_id`/`visibility`, columns `0009` adds — SQLite resolves trigger bodies lazily, so this would apply cleanly on `0008` alone and fail only at first use; the migration's own header comment now states the dependency explicitly.** | JANUS/KIN | BL-MEMOS-047 | migration applies cleanly on `main`'s shipped `0008`/`0009`, and is confirmed to depend on `0009` specifically (not merely `0008`); `scope_json` blanking accepted by the recreated trigger, still refused before `0010` (regression guard); rootpage-unchanged proof, same style as `0009`'s own drop+recreate acceptance | planned |

### PH-MEMOS-5 — Principal vaults and API-010 (TASK-MEMOS-008, SPR-MEMOS-05/06)

| ID | Epic | Title | Owner | Depends | Proof | Status |
|---|---|---|---|---|---|---|
| BL-MEMOS-060 | TASK-MEMOS-008 | **Fully scoped, design v0.6.0b §12.4.** `migrations/0011_principal_vaults.sql` (number assigned at merge per `DEC-MEMOS-14`/`37`, not pre-bound — concrete now that `0008`/`0009`/`0010` are confirmed merged): first-ever real use of `-- msp-migration: foreign-keys=off` on a populated database (`vaults` has four existing child tables — `vault_mounts`/`entities`/`promotions`/`links`); safe create/copy/drop/rename rebuild; widened `vault_type` `CHECK` (`principal_private`/`principal_passport`); per-type owner `CHECK`s exempting an erased row (`status='erased'`, `principal_id` blanked); `status` `CHECK ('active','erased')`; `decay_policy` column, type-pinned (`pinned` for passport, `ebbinghaus` otherwise); partial unique indexes on active rows per type; `trg_vaults_update_guard` (identity-pin, permits only the legacy `project_id` backfill and the future erasure transition); `trg_vault_mounts_refuse_principal_insert`/`_update` (DB-layer never-mountable enforcement) | JANUS/KIN | BL-MEMOS-057 | `migrate.test.mjs`: fresh-database case; populated-through-`0010`-database case; `vault_mounts`/`entities`/`promotions`/`links` `REFERENCES vaults` still name `vaults` post-rebuild (not a dropped `vaults_old`); an `INSERT`/`UPDATE` producing `vaults.status` outside `('active','erased')` refused; a direct `INSERT INTO vault_mounts` naming a `principal_private`/`principal_passport` `vault_id` refused before any application code runs | planned |
| BL-MEMOS-061 | TASK-MEMOS-008 | **Fully scoped, design v0.6.0b §5.2.** `VaultRegistry`: `provisionPrincipalPrivateVault`/`provisionPrincipalPassportVault` (lazy, idempotent, `this.#db.transaction(...)`, matching the three legacy `provision*Vault` methods' own shape); `isVaultAccessibleTo(vaultId, {workspaceId, agentId, tenantId, principalId, allowPassport})` gains two new branches ahead of the unchanged legacy ones; `mountVault` refuses `principal_private`/`principal_passport` (`MspRuntimeError`, `vault_scope_denied`) before ever calling `#insertMount`, ahead of `0011`'s own DB-layer trigger | KIN | BL-MEMOS-060 | registry unit tests for both new `provision*Vault` methods (idempotent re-call returns the same row, no second write); `isVaultAccessibleTo`'s four legacy branches unaffected (regression); `mountVault` on a principal-type `vault_id` refused at the JS layer, confirmed by mocking the DB layer so the trigger itself never fires | planned |
| BL-MEMOS-062 | TASK-MEMOS-008 | **Fully scoped, design v0.6.0b §5.3, §5.3.1.** API-010 `msp_vault_resolve`, built to match zuri-ai's shipped, unsigned `{actor, access_context, authorization}` caller (`msp-vault-resolver.js`, `origin/main@4ca28c1d`) exactly — no `grant`/`signature`; provisioning (episodic always, passport only under `authorization.allow_passport === true`) in one transaction; journal receipt with `principal_hmac` (no raw `principal_id`); response additive-only (`workspacePrivateVaultId`/`globalPrivateVaultIds`/`sharedVaultIds`/`permissions.{read,writePrivate,writeShared,policyVersion}` unchanged, plus new `principalPrivateVaultId`/`principalPassportVaultId`/`permissions.allowPassport`); typed error table (`validation_failed`/`vault_scope_denied`/`identity_hmac_unconfigured`) | KIN | BL-MEMOS-061 | contract tests against the exact request/response shapes in design §5.3; **same resolve returns the same vault**, including a genuine two-process race resolved via `idx_vaults_principal_private_active`'s unique index (loser re-`SELECT`s, no raw `SqliteError` surfaced); a mocked replay of zuri-ai's own `msp-vault-resolver.test.js` fixtures against a real `msp-server` process, confirming every field it asserts (`resolves.toEqual`, the four rejection cases) still holds with the additive fields present | planned |
| BL-MEMOS-063 | TASK-MEMOS-008 | **Fully scoped, design v0.6.0b §5.1.** API-009 `access_context` amendment (content specified now; the live edit to `docs/API-009-Persistent-Memory-Contract.md` is this item's own implementation-time deliverable per `DEC-MEMOS-47`, not a separate backlog item): optional `access_context` (`tenant_id`, `principal_id`, `agent_id?`, `workspace_id?`, `allow_passport?`), mandatory for principal vault types on all nine `msp_memory_*` tools — the three-way branch (vault-identifying tools / entity-id-only tools resolving entity→vault / `links_create`'s single-vault-after-the-existing-same-vault-refusal case) exactly as design §5.1 specifies; `msp_memory_decay_tick` gains `pinned: boolean`, always `0`/`[]` transitions for a `principal_passport` vault regardless of `dry_run`; `vault_scope_denied` broadened additively; two new codes `access_context_required`/`access_context_denied` | KIN/ATHER | BL-MEMOS-061 | `api-009-conformance.test.mjs`: absent `access_context` ⇒ unchanged for every legacy vault type, `access_context_required` for principal types; a mismatched tuple or a passport target missing `allow_passport` ⇒ `access_context_denied` (same code for both sub-cases, asserted as a deliberate non-oracle); each of the nine tools exercised, including `msp_memory_history`/`forget`/`links_list` (entity→vault resolution) and `msp_memory_links_create` (both endpoints, confirming no independent second check runs); `msp_memory_decay_tick`'s `pinned` field asserted on both `dry_run` arms | planned |
| BL-MEMOS-064 | TASK-MEMOS-008 | **Fully scoped, design v0.6.0b §5.4, §12.4.1.** `migrations/0012_contexts_access_scope.sql` (provisional, after `0011`): additive nullable `tenant_id`/`principal_id` on `contexts`, no rebuild; `msp_context_diff`/`audit`/`replay` require a matching `access_context` for a **scoped** row (both columns non-null), unaffected for a legacy row (both null); `include_payload` refused **unconditionally** for a scoped row, even with a correctly-matching `access_context` | KIN | BL-MEMOS-062 | `context-tools-ownership.security.mjs`: legacy-row case unaffected (regression); scoped-row case requires `access_context`, missing ⇒ `access_context_required`, mismatched ⇒ `access_context_denied`; `include_payload` refused on a scoped row even with a matching `access_context` (the specific case proving this is not merely the ordinary authorization check) | planned |
| BL-MEMOS-065 | TASK-MEMOS-008 | **Fully scoped, design v0.6.0b §5.5.** Multi-agent vault rules: two agents serving one person get distinct episodic vaults (falls out of `provisionPrincipalPrivateVault`'s own four-field key, `BL-MEMOS-061`, no separate enforcement code); passport readable only with `allow_passport`, checked on **every** path (resolve, §5.3, and every later direct vault-touching call via §5.1's `principal_passport` branch — not resolve alone); `global_private` agent vault structurally unreachable from any principal's own facts (no code path in §5.1/§5.2/§5.3 ever writes `tenant_id`/`principal_id` onto one) | KIN | BL-MEMOS-062 | vault multi-agent cases: two agents, same `(tenant_id, principal_id)`, different `agent_id` ⇒ two distinct `principalPrivateVaultId`s; a caller with a matching `tenant_id`/`principal_id` but no `allow_passport` claim on a *direct* `msp_memory_*` call against an already-known `principalPassportVaultId` (obtained once with `allow_passport` and reused later without it) is refused `access_context_denied`, proving the gate is re-checked per call, not cached from resolve time; a `global_private` vault never appears as any `access_context` branch's target | planned |
| BL-MEMOS-066 | TASK-MEMOS-008 | Suites: `principal-vault-scoping.security.mjs`, `provenance-ids-are-not-owners.security.mjs` (restored), `context-tools-ownership.security.mjs` (restored), decay-tick `pinned` case, `shared-scope-fail-closed.security.mjs` extension (principal-vault entities remain eligible for existing GKS promotion with no special-casing; `gks_provider_unconfigured` unaffected) — every case named in design §15's PH-MEMOS-5 rows | GHOST | BL-MEMOS-061..065 | suites green through the real process | planned |
| BL-MEMOS-067 | TASK-MEMOS-008 | Real-graph migration tests for `0011` and `0012`: fresh and populated `0001`–`0010` (the real, checksum-locked graph on `main`, not a hypothetical one per design §12.4); child `REFERENCES` (`vault_mounts`/`entities`/`promotions`/`links`) still name `vaults`; an unexpected `vaults.status` row refused; `0012`'s additive columns applied cleanly on top of `0011` | JANUS | BL-MEMOS-060, BL-MEMOS-064 | `migrate.test.mjs` cases (moved from WP-E0 per design v0.2.3b) | planned |
| BL-MEMOS-068 | TASK-MEMOS-008 | RKOI review; PR; merge | RKOI/COORD | BL-MEMOS-060..067, BL-MEMOS-105 | APPROVED 0 critical; merged | planned |
| BL-MEMOS-105 | TASK-MEMOS-008 | **Fully scoped, design v0.6.0b §5.3.1.** Cross-repo verification: read zuri-ai's `msp-vault-resolver.js`/`msp-vault-resolver.test.js`/`msp-vault-memory-port.test.js` (`origin/main@4ca28c1d`) against the actual `msp_vault_resolve` design (fields, error codes, `allow_passport` handling) rather than assuming compatibility — full match/mismatch table in design §5.3.1. Result: no wire-breaking mismatch found; two real gaps identified and resolved additively (`allow_passport` entirely absent from the shipped caller, `DEC-MEMOS-42`; `project_id` required by the caller but foreign to principal-vault ownership, `DEC-MEMOS-40`/design §5.3) rather than silently assumed compatible; one genuine follow-up gap filed as its own item, `BL-MEMOS-113` (below), and its own risk, `RSK-MEMOS-12` | RKOI/COORD | BL-MEMOS-062 | verification note recorded in the PR (design §5.3.1's own table); mismatches filed as their own risk (`RSK-MEMOS-12`) | planned |

### PH-MEMOS-6 — Consolidation, passport, cross-thread recall (TASK-MEMOS-009, SPR-MEMOS-07)

| ID | Epic | Title | Owner | Depends | Proof | Status |
|---|---|---|---|---|---|---|
| BL-MEMOS-070 | TASK-MEMOS-009 | Consolidation: CONFIRMED protected records and summary items become entities in the owner's episodic vault, under the owner's own access context only; `entity_provenance` table | KIN | BL-MEMOS-068 | "no fact lands where a direct upsert would be denied" case | planned |
| BL-MEMOS-071 | TASK-MEMOS-009 | Passport promotion policy: confidence and multi-episode thresholds, `allow_passport`, `passport_deferred` reported | KIN | BL-MEMOS-070 | policy tests | planned |
| BL-MEMOS-072 | TASK-MEMOS-009 | Context read extension: passport and episodic-recall slices gated by access context; cross-thread digest from the principal's own DIRECT threads only; reference-only receipt | KIN | BL-MEMOS-070, BL-MEMOS-064 | cross-thread digest case | planned |
| BL-MEMOS-073 | TASK-MEMOS-009 | Erasure extension to vaults: entity and history body tombstones, embeddings deleted, FTS re-indexed, vault rows erased and cleared in one UPDATE | KIN | BL-MEMOS-070, BL-MEMOS-053 | direct-database assertions incl. `entities_fts` MATCH | planned |
| BL-MEMOS-074 | TASK-MEMOS-009 | Suites: `consolidation-vault-scoping`, `cross-thread-digest-scoping`, `group-thread-private-context`, extended erasure | GHOST | BL-MEMOS-070..073 | suites green | planned |
| BL-MEMOS-075 | TASK-MEMOS-009 | RKOI review; PR; merge | RKOI/COORD | BL-MEMOS-070..074 | APPROVED 0 critical; merged | planned |

### PH-MEMOS-7 — Hardening and release (TASK-MEMOS-010, SPR-MEMOS-01 and SPR-MEMOS-08)

| ID | Epic | Title | Owner | Depends | Proof | Status |
|---|---|---|---|---|---|---|
| BL-MEMOS-080 | TASK-MEMOS-010 | Policy line in `docs/MIGRATION.md`: no foreign key into FTS5/rtree shadow tables, even when SQLite accepts it | ATHER | — | doc row; RKOI #19 advisory closed | planned |
| BL-MEMOS-081 | TASK-MEMOS-010 | rtree runner test uses `it.skipIf(!rtreeAvailable)` instead of a silent FTS5 fallback | JANUS | — | test shows skipped when the module is absent | planned |
| BL-MEMOS-082 | TASK-MEMOS-010 | Near-miss review follow-ups: document that header scanning stops after an exact line 1, with a test; rename the "not accepted via trim()" test title | JANUS | — | doc and test | planned |
| BL-MEMOS-083 | TASK-MEMOS-010 | End-to-end acceptance suite through the real process: 2 tenants × 3 principals × 2 agents × DIRECT and GROUP threads, covering resolve, append, context, record, summary, vault resolve, consolidation, erase | GHOST | BL-MEMOS-075 | suite green; isolation matrix recorded | planned |
| BL-MEMOS-084 | TASK-MEMOS-010 | Gate A re-baseline: "vault and thread isolation" row, suite counts, migration lineage 0008–0011 | ATHER | BL-MEMOS-083 | `docs/GATE-A.md` updated with evidence | planned |
| BL-MEMOS-085 | TASK-MEMOS-010 | Client release: `@freshair129/msp-client-js` version, CHANGELOG, env allowlist names, `npm run pack:client` clean | JANUS | BL-MEMOS-083 | pack dry-run output | planned |
| BL-MEMOS-086 | TASK-MEMOS-010 | Documentation closure: README, ARCHITECTURE layering for thread and vault surfaces, NOTES gaps closed (API-009 caller identity, context-tool ownership) | ATHER | BL-MEMOS-083 | frontmatter and CHANGELOG rows | planned |
| BL-MEMOS-087 | TASK-MEMOS-001 | Owner confirms or amends DEC-MEMOS-01..16; ADR status → accepted; amendments re-reviewed | OWNER/RKOI | BL-MEMOS-014 | ADR updated | done for DEC-MEMOS-01..35 (01..16 and 17..21 confirmed separately on 2026-09-14, 22..35 confirmed on 2026-09-15, no amendments); ADR status becomes accepted when BL-MEMOS-013 merges |
| BL-MEMOS-088 | TASK-MEMOS-010 | Release review and merge; tag the release | RKOI/COORD | BL-MEMOS-083..087 | GATE-MEMOS-7 met | planned |

### PH-MEMOS-8 — Channel activation (deferred; TASK-MEMOS-005..007)

| ID | Epic | Title | Owner | Depends | Proof | Status |
|---|---|---|---|---|---|---|
| BL-MEMOS-090 | TASK-MEMOS-005 | **Deduplicated against `BL-MEMOS-107` (RKOI stage-2 review round 1, warning 6): this item owns only the transport/activation-gate half, never the grant-shape change.** zuri-ai's transport allowlist passes `MSP_THREAD_SERVICE_KEY` and `MSP_IDENTITY_HMAC_KEY`; this item is also the named activation gate `RSK-MEMOS-01`/`DEC-MEMOS-17` point at — the grant fields themselves (`agentId`/`workspaceId`/`nonce`/`assertAgents`) are `BL-MEMOS-107`'s scope alone, not restated here | zuri-ai owner | BL-MEMOS-088 | zuri-ai PR; allowlist updated | deferred |
| BL-MEMOS-091 | TASK-MEMOS-005 | Real-process contract proof from zuri-ai over stdio; settle raw `externalThreadId` and `PENDING_INBOUND` handling | zuri-ai owner | BL-MEMOS-090 | acceptance test | deferred |
| BL-MEMOS-092 | TASK-MEMOS-003 | zuri-ai calls the lifecycle tool on Person relink/merge and Membership revocation. **Shape now specified (design v0.5.0b §7.1)**: `msp_thread_participant_lifecycle`'s `close_for_relink` action, with **both** `assertParticipants: true` and the new `assertRelink: true` (`DEC-MEMOS-23`) on the grant — neither claim alone is sufficient | zuri-ai owner | BL-MEMOS-090 | cross-repo test | deferred |
| BL-MEMOS-093 | TASK-MEMOS-004 | zuri-ai PDPA erasure calls MSP erasure. **Shape now specified (design v0.5.0b §11.2)**: `msp_thread_principal_erase` with `dataSubjectAdmin: true` (`DEC-MEMOS-25`), since zuri-ai's own erasure flow is expected to run as an administrative process rather than a live per-principal user grant | zuri-ai owner | BL-MEMOS-090 | cross-repo test | deferred |
| BL-MEMOS-094 | TASK-MEMOS-006 | LINE canary on one DIRECT account with rollback runbook | zuri-ai owner/OWNER | BL-MEMOS-091..093 | canary evidence | deferred |
| BL-MEMOS-095 | TASK-MEMOS-007 | LINE worker hosts the MSP summary worker | zuri-ai owner | BL-MEMOS-094 | summaries cited in production | deferred |
| BL-MEMOS-096 | — | Re-open decision: owner lifts the PH-MEMOS-8 deferral | OWNER | BL-MEMOS-088 | owner instruction | deferred |
| BL-MEMOS-106 | TASK-MEMOS-005 | zuri-ai's API-009 port sends `access_context` on `msp_memory_*` calls (**citation corrected, PH-MEMOS-5 scoping: design v0.6.0b §5.1**, which now fully specifies the amendment this item's caller must adopt) — required before a principal vault is usable in production | zuri-ai owner | BL-MEMOS-063 | zuri-ai PR; `api-009-conformance.test.mjs` case exercised against a real zuri-ai payload | deferred |
| BL-MEMOS-113 | TASK-MEMOS-005 | **New (PH-MEMOS-5 scoping, cross-repo verification `BL-MEMOS-105`, `DEC-MEMOS-48`).** zuri-ai's `msp-vault-resolver.js`/`validateVaultSet` must be extended to read and forward `msp_vault_resolve`'s new `principalPrivateVaultId`/`principalPassportVaultId`/`permissions.allowPassport` response fields (design v0.6.0b §5.3), and its `authorizationFacts()` must add `allow_passport` to the `authorization` object it sends — today `validateVaultSet` silently drops all three new response fields (confirmed by reading its source, design §5.3), and with no `allow_passport` sent, `msp_vault_resolve` always answers `principalPassportVaultId: null` for every zuri-ai call, safe but production-unusable for the passport tier | zuri-ai owner | BL-MEMOS-062 | zuri-ai PR; a real zuri-ai payload round-trips `principalPrivateVaultId`/`principalPassportVaultId`/`permissions.allowPassport` through the updated `msp-vault-resolver.js` | deferred |
| BL-MEMOS-107 | TASK-MEMOS-005 | **Sole owner of the grant-shape change (deduplicated against `BL-MEMOS-090`, RKOI stage-2 review round 1, warning 6)** — `assertParticipants` is already shipped in stage 1 and needs no zuri-ai change (see `BL-MEMOS-023`): zuri-ai's signer starts sending `agentId`, `workspaceId` and a `nonce` (**≥128 random bits, `DEC-MEMOS-20`**) on the grant, and sets `assertAgents` where design §8.1 requires it, once stage 2 exists (ADR `RSK-MEMOS-01`). **New (RKOI stage-2 review round 1, warning 6): zuri-ai's outbound append's `agentId` must equal the `speakerId` it already sends** (`'zuri-line-agent'`), per design §8.2's `AGENT`-speaker rule. **`DEC-MEMOS-17`: no compatibility flag — every zuri-ai call to a stage-2-gated thread tool fails closed until this PR lands**, not merely "may not work"; nothing production-facing depends on that working today, since activation is gated behind `BL-MEMOS-090` regardless | zuri-ai owner | BL-MEMOS-090 | zuri-ai PR; cross-repo acceptance test; `test:cross-zuri`'s `DEC-MEMOS-17` cases — before this PR lands the suite asserts the unmodified grant is refused with a typed grant error, after it lands the same suite must pass; a separate case proves only that MSP *accepts* the wrapped shape, labelled as such, not that this PR has actually landed | deferred |

## 6. Decisions

`DEC-MEMOS-01` through `DEC-MEMOS-16` were **confirmed by the owner on
2026-09-14** ("ยืนยัน"), recorded on `docs/memos-001-adr-design-plan`
at commit `214a7d2` — `BL-MEMOS-014` and `BL-MEMOS-087` are both **done**
for that range. `DEC-MEMOS-17` through `DEC-MEMOS-21` below are new
(PH-MEMOS-3 stage-2 scoping, v0.1.7b/0.1.8b/0.1.9b — `19`/`20` promoted
from unnumbered prose in RKOI's stage-2 review round 1, `21` promoted in
round 2). **DEC-MEMOS-17..21 were confirmed by the owner on 2026-09-14** in a separate, later answer ("ยืนยัน DEC 17-21"). `DEC-MEMOS-22`
through `DEC-MEMOS-35` below are new (PH-MEMOS-4 scoping, v0.1.12b/0.1.13b —
participant lifecycle, erasure, retention, export; `34`/`35` added in
v0.1.13b answering RKOI's PH-MEMOS-4 review). **DEC-MEMOS-22..35 were
confirmed by the owner on 2026-09-15** ("ยืนยัน"), the same way `17..21`
were confirmed on 2026-09-14. `DEC-MEMOS-36` through `DEC-MEMOS-48` below
are new (PH-MEMOS-5 scoping, v0.1.18b — principal vaults, API-010,
API-009 `access_context` amendment, scoped `contexts` receipts,
multi-agent vault rules). **`DEC-MEMOS-36..48` are NOT yet confirmed by
the owner** — unlike every earlier range in this table, this is the first
design pass for this range; `BL-MEMOS-014`/`BL-MEMOS-087` do not cover it.

| ID | Decision | Adopted default | Affects |
|---|---|---|---|
| DEC-MEMOS-01 | API numbering | API-010 = `msp_vault_resolve` (zuri-ai ADR-022); thread surface = API-011 | BL-MEMOS-030, BL-MEMOS-062 |
| DEC-MEMOS-02 | Canonical thread surface | The branch's `msp_thread_*` tools; no field zuri-ai sends changes except RSK-MEMOS-01 | PH-MEMOS-2, PH-MEMOS-3 |
| DEC-MEMOS-03 | Instances / agent leg | Dropped for server channels; the signed per-room grant is the recorded relation; agents are modelled by `thread_agents` | BL-MEMOS-041 |
| DEC-MEMOS-04 | Identity changes | MSP provides a lifecycle tool; callers wire it later | BL-MEMOS-050, BL-MEMOS-092 |
| DEC-MEMOS-05 | Erasure before activation | Erasure (PH-MEMOS-4, extended in PH-MEMOS-6) gates any channel activation | GATE-MEMOS-8 |
| DEC-MEMOS-06 | Room refs at rest | Stored as HMAC under `MSP_IDENTITY_HMAC_KEY` | BL-MEMOS-021 |
| DEC-MEMOS-07 | Migration order | **Corrected 2026-09-14 (DEC-MEMOS-14 supersedes the original wording):** `0008` is thread memory, stage 1 only. Migration numbers after `0008` are assigned in merge order, not pre-bound — the stage-2 multi-agent migration and the principal-vaults migration each get whatever number the runner assigns when they land, expected in that order per the phase plan but not guaranteed | BL-MEMOS-020, BL-MEMOS-041, BL-MEMOS-060 |
| DEC-MEMOS-08 | Thread memory vs vaults | Thread-scoped memory stays in thread tables; CONFIRMED items consolidate into principal vaults under the owner's context | BL-MEMOS-070 |
| DEC-MEMOS-09 | Caller `now` | Test-only (`MSP_TEST_CLOCK=1`) | BL-MEMOS-026 |
| DEC-MEMOS-10 | Extractive fallback | Not built; `coverageGap` is the mechanism | PH-MEMOS-6 |
| DEC-MEMOS-11 | Relink | A `DIRECT` thread whose person changes is closed; the channel binding mints a new thread for the new principal; binding uniqueness is `ACTIVE`-scoped only; the new principal inherits no history | BL-MEMOS-022, BL-MEMOS-050 |
| DEC-MEMOS-12 | First membership | The first `HUMAN` membership is created by a `HUMAN` append whose `speaker_id === grant.principalId`; `resolve` carries no `participants` field; every other participant change requires `assertParticipants` | BL-MEMOS-023 |
| DEC-MEMOS-13 | Package placement | The thread store stays in `msp-core`; no separate `msp-thread-memory` package | BL-MEMOS-025 |
| DEC-MEMOS-14 | Agent timing | Stage 1 (`0008`) has no `thread_agents`, no required `agentId`, no record `agent_id`/`visibility`, no `grant_nonces`; stage 2 adds them in its own later migration | BL-MEMOS-040, BL-MEMOS-041, BL-MEMOS-048 |
| DEC-MEMOS-15 | Assurance self-upgrade needs no claim | A later append's `PENDING → VERIFIED` is accepted with no `assertParticipants` only when `speaker_id === grant.principalId`, `speaker_kind === HUMAN`, `person_id ∈ {null, grant.principalId}` for both the value sent and the stored participant row, and the row is that principal's own current membership; the upgrade closes the old row (`left_at`) and inserts the new VERIFIED row in one transaction, because the append-only trigger allows only `left_at` to change; `VERIFIED → PENDING` is silently ignored, not stored and not refused; every other change still needs `assertParticipants`; MSP's row stays VERIFIED after zuri-ai de-verifies someone, so until the lifecycle tool exists revocation relies on zuri-ai no longer setting `readPrivate` | BL-MEMOS-023 |
| DEC-MEMOS-16 | `channel_type` mismatch on resolve | **New (RKOI stage-1 code review round 2 on code commit `95629e2`).** A `msp_thread_resolve` whose `channel_type` differs from the `channel_type` already stored on the existing `ACTIVE` thread for the same tenant, account and room hash is refused with the typed `conflict` error — it never returns the other channel's thread, and never mints a second thread for the same `(tenant_id, channel_account_id, external_room_ref_hmac)` triple either. Consequence the owner confirms: a second channel type can never get its own thread for the same account and room ref. Replaces the design's earlier claim (§6.2) that the same tenant/account/room-hash triple "names the same room regardless of which transport label a given call happens to carry" — that claim was wrong on its own terms. The room hash stays three segments (no `channel_type` added to it); `channel_type` remains a pinned column on `threads` (`trg_threads_pin_identity`) | BL-MEMOS-021, GATE-MEMOS-2 |
| DEC-MEMOS-17 | No zuri-ai compatibility flag for stage 2 | **New (PH-MEMOS-3 stage-2 scoping, v0.1.7b, confirmed by the owner 2026-09-14).** Stage 2's `agentId`/`workspaceId`/`nonce` requirements make zuri-ai's current grant fail closed the moment stage-2 verification ships, with no interim compatibility mode. Adopted because channel activation is already gated behind `BL-MEMOS-090` (owner direction, 2026-09-14: no LINE OA yet) — nothing production-facing depends on the old shape continuing to work once stage 2 exists | BL-MEMOS-046, BL-MEMOS-047, BL-MEMOS-107, RSK-MEMOS-01 |
| DEC-MEMOS-18 | Worker identity for stage 2 | **Revised (RKOI stage-2 review round 1, v0.1.8b, confirmed by the owner 2026-09-14).** The worker acts under its own `agentId`, never impersonating the serving agent: it attaches through `msp_thread_resolve` with `assertAgents: true`, using its own grant's room claims; a worker-only grant (`operator`, no reader/writer flags) never mints a thread, `not_found` if the room has no `ACTIVE` thread. `msp_session_compaction_claim`/`commit`/`retry` require the worker's own `grant.agentId` to be a current agent of the job's resolved thread, reusing the `thread_agents` mechanism with no new capability flag; `msp_session_sweep` alone is exempt from "current" (it precedes thread resolution) but still requires `agentId`/`workspaceId` present. Rejected a dedicated gate-exempt worker role as a duplicate authorization path for no proven benefit. Widens nothing: the worker's tenant service key could already assert any claim. **Revocation wording withdrawn**: "ending a `thread_agents` row" is not revocation — detach is self-only and reversible via `assertAgents`; real revocation is Tier 1 withholding grants or a key rotation | BL-MEMOS-041, BL-MEMOS-042, GATE-MEMOS-3 |
| DEC-MEMOS-19 | Default record visibility | **New (RKOI stage-2 review round 1, v0.1.8b, confirmed by the owner 2026-09-14, promoted from unnumbered prose).** The default `visibility` for a new `protected_memory_records` row is `THREAD` — shared with every current agent of the thread unless the recording agent opts into `AGENT`-only. Matches what every legacy stage-1 row backfills to | BL-MEMOS-043 |
| DEC-MEMOS-20 | Nonce rules | **New (RKOI stage-2 review round 1, v0.1.8b, confirmed by the owner 2026-09-14, promoted from unnumbered prose).** A signed nonce carries at least 128 random bits and at most 128 characters; the anti-replay key is `(tenant_id, nonce)`, never global; the opportunistic prune batch is 200 rows, bounded, run on every nonce-consuming insert. The random-bit floor is a signer-side requirement so agents sharing one tenant cannot collide into a spurious `grant_replayed` refusal | BL-MEMOS-048, BL-MEMOS-104, BL-MEMOS-107 |
| DEC-MEMOS-21 | `agentId`/`workspaceId` bound | **New (RKOI stage-2 review round 2, v0.1.9b, confirmed by the owner 2026-09-14, promoted from unnumbered prose).** Both claims are non-empty strings bounded at 128 characters, with no further charset constraint — MSP has no agent/workspace identity registry of its own, mirroring `principalId`'s existing treatment as an opaque Tier-1-owned string; the bound exists only to cap storage and `payloadHash` cost | BL-MEMOS-040, BL-MEMOS-046 |
| DEC-MEMOS-22 | `leave` claim, always | **New (PH-MEMOS-4 scoping, v0.1.12b), confirmed by the owner 2026-09-15.** `leave` always requires `assertParticipants`, self or third-party `speaker_id`, no self-service exception; never transitions `threads.status` | BL-MEMOS-050, BL-MEMOS-052, GATE-MEMOS-4 |
| DEC-MEMOS-23 | `close_for_relink`'s distinct claim | **New (PH-MEMOS-4 scoping, v0.1.12b), confirmed by the owner 2026-09-15.** `assertRelink` (new boolean), additive to `assertParticipants`, never `operator`; `DIRECT`-only; closes the current HUMAN row and the thread in one transaction with no schema change | BL-MEMOS-050, BL-MEMOS-052, GATE-MEMOS-4 |
| DEC-MEMOS-24 | Agent detach needs no new claim | **New (PH-MEMOS-4 scoping, v0.1.12b), confirmed by the owner 2026-09-15.** The existing generic agent-currency gate already makes `msp_thread_agent_detach` self-only and pre-validates the row exists | BL-MEMOS-051 |
| DEC-MEMOS-25 | `data_subject_admin` is a grant flag pair | **New (PH-MEMOS-4 scoping, v0.1.12b), confirmed by the owner 2026-09-15.** Resolves the ADR's own open question: `dataSubjectAccess` (base) + `dataSubjectAdmin` (additive, cross-principal), not a Membership role | BL-MEMOS-053, BL-MEMOS-055, BL-MEMOS-093 |
| DEC-MEMOS-26 | Tool names and retention's capability | **New (PH-MEMOS-4 scoping, v0.1.12b), confirmed by the owner 2026-09-15.** `msp_thread_principal_erase`/`msp_thread_principal_export`/`msp_thread_retention_tick`; retention reuses `operator` via an explicit guard name check | BL-MEMOS-053..055 |
| DEC-MEMOS-27 | Erasure idempotency key | **New (PH-MEMOS-4 scoping, v0.1.12b), confirmed by the owner 2026-09-15.** Caller-supplied opaque key, 1–128 chars, scoped `(tenant_id, idempotency_key)`; same-principal replay is a no-op, different-principal replay is `conflict` | BL-MEMOS-053 |
| DEC-MEMOS-28 | `erasure_receipts` stores the raw principal id | **New (PH-MEMOS-4 scoping, v0.1.12b), confirmed by the owner 2026-09-15.** Consistent with every other content table; W5 pseudonymization stays scoped to the journal entry only | BL-MEMOS-053 |
| DEC-MEMOS-29 | Retention scope for this phase | **New (PH-MEMOS-4 scoping, v0.1.12b), confirmed by the owner 2026-09-15.** One deployment-wide `MSP_THREAD_RETENTION_DAYS` horizon, no per-tenant policy table yet; 200-row/table/call bound | BL-MEMOS-054, RSK-MEMOS-06 |
| DEC-MEMOS-30 | Export excludes tombstoned content | **New (PH-MEMOS-4 scoping, v0.1.12b), confirmed by the owner 2026-09-15.** Including the exporting principal's own erased rows — erasure stays permanent for export too | BL-MEMOS-055 |
| DEC-MEMOS-31 | Export ignores agent visibility | **New (PH-MEMOS-4 scoping, v0.1.12b), confirmed by the owner 2026-09-15.** `visibility` governs agent-to-agent confidentiality, not the data subject's own access right | BL-MEMOS-055 |
| DEC-MEMOS-32 | Erasure/export per-table selection | **New (PH-MEMOS-4 scoping, v0.1.12b), confirmed by the owner 2026-09-15. Corrected (v0.1.13b, RKOI PH-MEMOS-4 review, `DEC-MEMOS-34`; corrected again v0.1.14b, RKOI PH-MEMOS-4 review round 2, CRITICAL 2/WARNING 4).** Operationalizes design §11.1 exactly — own `speaker_id` for messages, asserter-or-subject for records, `DEC-MEMOS-34`'s sole-ever-HUMAN-participant test (now two independent ANDed conditions: participant-history count, and no disqualifying `UNKNOWN`/`OPERATOR` message) for summaries/`thread_delivery_receipts`; `thread_pending_deliveries` removed from erasure's scope | BL-MEMOS-053, BL-MEMOS-055 |
| DEC-MEMOS-33 | Unknown principal is a trivial success | **New (PH-MEMOS-4 scoping, v0.1.12b), confirmed by the owner 2026-09-15.** Never `not_found`, on both erase and export — avoids a new cross-principal existence oracle | BL-MEMOS-053, BL-MEMOS-055, RSK-MEMOS-09 |
| DEC-MEMOS-34 | Sole-ever-HUMAN-participant scoping for summaries/delivery receipts | **New (PH-MEMOS-4 scoping, v0.1.13b, RKOI PH-MEMOS-4 review response, CRITICAL 2/3), confirmed by the owner 2026-09-15. Revised (v0.1.14b, RKOI PH-MEMOS-4 review round 2, CRITICAL 2/WARNING 4).** Erasure/export act on `session_summaries`/`thread_delivery_receipts` only for a thread where **both** hold: the principal is, across its entire participant history, the thread's only-ever `HUMAN` participant (checked as two separate counts — `speaker_id` and non-null `person_id`, either exceeding 1 disqualifies, WARNING 4), **and** `thread_messages` carries no row with `speaker_kind NOT IN ('HUMAN', 'AGENT')` anywhere on the thread (new, CRITICAL 2 — `UNKNOWN`/`OPERATOR` speakers post messages with no participant row at all, so the participant-only test alone missed them; `AGENT` stays excluded as the assistant's own reply). A GROUP/ROOM thread failing either condition is left untouched (erasure) or excluded entirely (export) — a conservative under-erasure default, stated as such | BL-MEMOS-053, BL-MEMOS-055, BL-MEMOS-056, GATE-MEMOS-4 |
| DEC-MEMOS-35 | `dry_run` is fully read-only | **New (PH-MEMOS-4 scoping, v0.1.13b, RKOI PH-MEMOS-4 review response, WARNING 6), confirmed by the owner 2026-09-15. Revised (v0.1.14b, RKOI PH-MEMOS-4 review round 2, WARNING 6).** `msp_thread_retention_tick`'s `dry_run: true` consumes no nonce (stands); it **does** write a journal entry, like `dry_run: false` (aggregate counts and the `dry_run` flag only, no per-row content) — `msp_thread_context`'s no-journal precedent does not transfer, since retention's `dry_run` is tenant-wide and, with no nonce, would otherwise be callable unboundedly with zero audit trail. Only the nonce exemption is dry-run-specific | BL-MEMOS-054, GATE-MEMOS-4 |
| DEC-MEMOS-36 | Principal vault owner tuples / `decay_policy` | **New (PH-MEMOS-5 scoping, v0.1.18b), pending owner confirmation.** `principal_private` = `tenant_id, principal_id, agent_id, workspace_id`, ebbinghaus decay; `principal_passport` = `tenant_id, principal_id` only, pinned (no decay); pinned per type by a `CHECK`, not a caller-chosen setting | BL-MEMOS-060, GATE-MEMOS-5 |
| DEC-MEMOS-37 | Migration numbers `0011`/`0012` | **New (PH-MEMOS-5 scoping, v0.1.18b), pending owner confirmation.** Principal vaults = `0011`; scoped `contexts` receipts = `0012` — both provisional per `DEC-MEMOS-14`'s merge-order rule, concrete now that `0008`–`0010` are confirmed merged | BL-MEMOS-060, BL-MEMOS-064 |
| DEC-MEMOS-38 | `vaults` rebuild strategy | **New (PH-MEMOS-5 scoping, v0.1.18b), pending owner confirmation.** `foreign-keys=off` directive, safe create/copy/drop/rename order; per-type owner `CHECK`s exempt an erased row in advance; the erasure transition itself stays out of this phase's scope (PH-MEMOS-6) | BL-MEMOS-060 |
| DEC-MEMOS-39 | Never-mountable enforcement | **New (PH-MEMOS-5 scoping, v0.1.18b), pending owner confirmation.** Two `vault_mounts` triggers (DB layer) plus `VaultRegistry#mountVault`'s own refusal (JS layer), deliberate defense in depth; a separate `vaults` identity-pin trigger permits only the legacy `project_id` backfill and the future erasure transition | BL-MEMOS-060, BL-MEMOS-061 |
| DEC-MEMOS-40 | `msp_vault_resolve`'s request matches zuri-ai's shipped caller | **New (PH-MEMOS-5 scoping, v0.1.18b), pending owner confirmation.** No `grant`/`signature`, unsigned, matching `msp-vault-resolver.js` (`origin/main@4ca28c1d`) exactly — same stdio-only trust boundary already accepted for API-011 (`RSK-MEMOS-05`) | BL-MEMOS-062, BL-MEMOS-105 |
| DEC-MEMOS-41 | `msp_vault_resolve`'s response is additive-only | **New (PH-MEMOS-5 scoping, v0.1.18b), pending owner confirmation.** Legacy fields unchanged in shape/casing; new principal-vault fields camelCase, silently dropped by the shipped, unmodified `validateVaultSet` until `BL-MEMOS-113` lands | BL-MEMOS-062, BL-MEMOS-113 |
| DEC-MEMOS-42 | `allow_passport` gating is safe by default | **New (PH-MEMOS-5 scoping, v0.1.18b), pending owner confirmation.** Absent or not exactly `true` ⇒ no passport vault provisioned/returned; the episodic vault resolves on every well-formed call with no gating flag | BL-MEMOS-062, BL-MEMOS-065 |
| DEC-MEMOS-43 | API-009 `access_context` shape | **New (PH-MEMOS-5 scoping, v0.1.18b), pending owner confirmation.** One flat, snake_case object reusing `msp_vault_resolve`'s own field names; mandatory only for a principal-vault-type target; entity-id-only tools resolve entity → vault first; `links_create` needs no independent second check | BL-MEMOS-063 |
| DEC-MEMOS-44 | Error-code vocabulary for the amendment | **New (PH-MEMOS-5 scoping, v0.1.18b), pending owner confirmation.** `vault_scope_denied` broadened additively (same code); two new codes, `access_context_required` (absent) and `access_context_denied` (present but wrong, including a passport target missing `allow_passport`, deliberately the same code as a tuple mismatch) | BL-MEMOS-063 |
| DEC-MEMOS-45 | `msp_memory_decay_tick`'s `pinned` field | **New (PH-MEMOS-5 scoping, v0.1.18b), pending owner confirmation.** New `pinned: boolean` response field; a `principal_passport` vault always reports zero transitions regardless of `dry_run` | BL-MEMOS-063 |
| DEC-MEMOS-46 | Scoped `contexts` receipts | **New (PH-MEMOS-5 scoping, v0.1.18b), pending owner confirmation.** Nullable `tenant_id`/`principal_id` via a plain `ALTER TABLE`; both-or-neither enforced at the contracts layer, not a DB `CHECK`; `include_payload` refused unconditionally for a scoped row | BL-MEMOS-064 |
| DEC-MEMOS-47 | API-009 contract file's own edit deferred | **New (PH-MEMOS-5 scoping, v0.1.18b), pending owner confirmation.** The live edit to `docs/API-009-Persistent-Memory-Contract.md` is `BL-MEMOS-063`'s own implementation-time deliverable, not part of this design pass — matches the precedent already set for API-011's contract file | BL-MEMOS-063 |
| DEC-MEMOS-48 | New cross-repo item `BL-MEMOS-113` | **New (PH-MEMOS-5 scoping, v0.1.18b), pending owner confirmation.** zuri-ai's `validateVaultSet` must be updated to read/forward the new principal-vault response fields (and `authorizationFacts()` must add `allow_passport`) before they are usable in production; deferred to PH-MEMOS-8 like `BL-MEMOS-106`/`092`/`093` | BL-MEMOS-105, BL-MEMOS-113, RSK-MEMOS-12 |

Two earlier open design questions carry over and are tracked as risks rather than defaults, because no recommendation was agreed: tombstone retention horizon (design v0.2.3b §19.7 — `DEC-MEMOS-29`/`BL-MEMOS-054` is a partial, mechanism-only close, not a full one) and identity key presence at startup (§19.8). A third, new one: whether `grant_nonces` should move into `0008` instead of stage 2, closing the stage-1 nonce gap RKOI's ruling 3 and DEC-MEMOS-14 leave open together (design §19).

## 7. Risks

| ID | Risk | Likelihood / impact | Mitigation | Owner |
|---|---|---|---|---|
| RSK-MEMOS-01 | Multi-agent and the corrected design require several zuri-ai wire changes before any activation: (1) `agentId` and `workspaceId` become **required** grant fields in stage 2; (2) a `nonce` is required on every mutating call except append once `grant_nonces` exists, **carrying at least 128 random bits (`DEC-MEMOS-20`)** — a short or low-entropy nonce risks colliding with another of zuri-ai's own agents in the same tenant and producing a spurious `grant_replayed` refusal; (3) `assertAgents` must be set to join a thread the caller's agent did not create; (4) **a caller for `close_for_relink` — still a real, open cross-repo change**, zuri-owned, on the activation gate (RKOI round two, reaffirmed round three). **The mechanism, stated directly here (RKOI round four — previously only pointed at from the ADR and design §19 without actually being recorded in this row): a zuri-ai account merge into an existing Person changes that Person's `personId`. The next append from the merged account passes DEC-MEMOS-12's first-membership check (§7 rule 2), but the thread's *lifetime* single-`HUMAN` trigger (§6.3) still refuses a second distinct `HUMAN` speaker on that `DIRECT` thread. Every later append from the merged account then fails closed until `BL-MEMOS-092`'s relink caller exists — and the merged Person never inherits the old thread's history in the meantime, by design (DEC-MEMOS-11).** **Updated (PH-MEMOS-4 scoping, design v0.5.0b §7.1): the caller's exact grant shape is now specified, not just its existence** — `BL-MEMOS-092`'s caller must send **both** `assertParticipants: true` and the new `assertRelink: true` (`DEC-MEMOS-23`) on `close_for_relink`; neither claim alone is sufficient, and `operator` never substitutes for either. **Extended (RKOI PH-MEMOS-4 review, WARNING 3): the full generic thread-bound gate's demands apply too, not only the two participant claims** — `audienceKind === 'DIRECT'`, a `channelAccountId`/`externalRoomRef` pair re-hashing to the old thread's stored HMAC, `agentId`/`workspaceId`, a `nonce`, and an open `thread_agents` row for the calling agent; `BL-MEMOS-092`'s caller needs the complete shape, not just the additive claims this decision layers on top. (5) **the assurance-upgrade caller — corrected (RKOI round three): resolved MSP-side by DEC-MEMOS-15, no zuri-ai change needed for the normal case.** `assertParticipants` itself is already shipped in stage 1 for the cases DEC-MEMOS-12/15 cover; **the blanket sentence previously here claiming it "needs no zuri-ai change" is removed**, because it read as contradicting items 4 and 5 above — items 4 and 5 are about *separate* callers (relink, and the pre-DEC-MEMOS-15 upgrade path), not about `assertParticipants` itself. **(6) New (RKOI stage-2 review round 1, warning 6): zuri-ai's outbound append's `agentId` must equal the `speakerId` it already sends (`'zuri-line-agent'`), under design §8.2's `AGENT`-speaker rule. (7) `DEC-MEMOS-17`, stated directly here: stage 2 has no compatibility flag — every zuri-ai call to a stage-2-gated thread tool fails closed until `BL-MEMOS-107` lands, not merely "may not work as well."** None of these renames or reshapes a field zuri-ai already sends on the six frozen calls. | certain / medium | Full list also recorded in `docs/ADR-MSP-MEMORY-OS-MULTI-USER-MULTI-AGENT.md`'s "Cross-repo changes stage 2 requires of zuri-ai"; tracked as `BL-MEMOS-107` (the sole owner of every grant-shape item above — 1, 2, 3, 6 — one zuri-ai PR, deduplicated against `BL-MEMOS-090`, which owns only the transport allowlist and the activation gate itself), `BL-MEMOS-092` (item 4, the relink/merge caller zuri-ai owns); item 5 needs no BL id — DEC-MEMOS-15 closes it; item 7 needs no BL id — it is a fact about `BL-MEMOS-107`'s own deadline, not a separate deliverable; no activation until then (PH-MEMOS-8 deferred) | COORD |
| RSK-MEMOS-02 | Adopted defaults overturned by the owner after implementation | low–medium / high for DEC-MEMOS-01, -02, -07, -12, -15 | Confirmation requested early (BL-MEMOS-014); nothing merged past PH-MEMOS-2 changes a shipped migration without review | COORD/OWNER |
| RSK-MEMOS-03 | **Still open after round four's docs approval — an approved design is not the same as a verified implementation.** Design v0.3.0b and the stage-1 code diverged (RKOI's round-one review, 3 criticals). Design v0.3.1b was rebuilt from RKOI's prose description of the branch's shapes but still did not match the shipped code on several wire values (round-two review, commit `92cb591`). Design v0.3.2b read `feat/memos-002-thread-memory` directly — and round three (commit `6d1a801`) *still* found one wrong value (the delivery grant's real claim set), because v0.3.2b's fix reasoned about what a delivery grant would plausibly need rather than reading zuri-ai's actual signer directly. Design v0.3.3b/v0.3.4b read zuri-ai's real signer code directly for that claim, and round four (commit `1c4a62f`) approved the docs with 0 critical — but a cross-room scope gap with no backlog row at all (`BL-MEMOS-111`) still surfaced in the same round, on the code side. **This risk stays open, not closed, until `BL-MEMOS-033`'s own code review closes it** — repeated rounds of prose/inference-vs-code drift is exactly the failure mode this risk names, and a docs approval does not retire it. **Stage-1 code review round 2 (commit `445bd90`) proves the point again**: the docs-approved room-hash comparison (`BL-MEMOS-111`, folded into v0.3.4b/v0.1.4b) turned out to be insufficient on the shipped code itself — comparing the hash when present is not the same as requiring it be present at all — found only by reading `thread-guard.mjs` directly, not by re-reading the design. | medium / medium | Design v0.3.5b reads the shipped code directly (`thread-guard.mjs`), not an inference about it; `BL-MEMOS-033` (now also running `BL-MEMOS-110`'s cross-repo contract test against a real `origin/main` extract, and depending on `BL-MEMOS-111`) is the actual closing event, not a design revision | RKOI |
| RSK-MEMOS-04 | Migration 0008 lands and is later found wrong; checksum-locked lineage needs a corrective migration | low / high | Real-graph fresh and populated tests (BL-MEMOS-032); RKOI gate before merge; nothing past 0007 has shipped yet | JANUS |
| RSK-MEMOS-05 | A single all-tenant `MSP_THREAD_SERVICE_KEY`; capability flags are unverified Tier-1 claims | certain / high if transport widens | Trust boundary stays stdio-only (design §13.1); any network transport re-opens the design | RKOI |
| RSK-MEMOS-06 | Tombstoned rows kept indefinitely may not satisfy a tenant's PDPA position | unknown / medium | **Partially mitigated (PH-MEMOS-4 scoping): `BL-MEMOS-054`/`msp_thread_retention_tick` ships the tick *mechanism* against a uniform, deployment-wide `MSP_THREAD_RETENTION_DAYS` horizon (`DEC-MEMOS-29`) — a real, callable reduction in how long content stays readable, though the row itself is retained forever as a blanked audit stub (this schema never deletes rows). A richer, per-tenant-configurable policy remains open** — owner question carried from design §19.7. **Consequence stated explicitly (RKOI PH-MEMOS-4 review round 2, WARNING 5, same risk id, not a new one)**: `DEC-MEMOS-34`'s sole-ever-`HUMAN` test can only ever move a thread from "qualifies" to "disqualified," never back — once a thread that was sole-ever-`HUMAN` later gains a second distinct `HUMAN` participant (or a disqualifying `UNKNOWN`/`OPERATOR` message), that principal's own solo-era `session_summaries`/`thread_delivery_receipts` become **permanently non-erasable and non-exportable** by this phase's tools, a real right-to-erasure/right-to-access limitation, not merely a conservative scope note; resolve before GATE-MEMOS-7 | OWNER |
| RSK-MEMOS-07 | Identity key absent at startup leaves the thread and vault surfaces dark without a loud signal | medium / medium | Design §6.2 default (startup diagnostic, `msp_ping` report, opt-in fail-closed boot); resolve before GATE-MEMOS-7 | OWNER |
| RSK-MEMOS-08 | zuri-ai roadmap ids and revisions move fast (#379, #381, #382 each collided with PR #380) | high / low | Fetch and rebase immediately before any zuri-ai push; TASK-MEMOS family kept separate from TASK-ZAI | COORD |
| RSK-MEMOS-09 | **New (RKOI stage-1 code review round 2 on code commit `95629e2`).** Caller-supplied ids that are globally unique still answer a foreign tenant's id differently from an unused one — a cross-tenant existence oracle at the outcome level. **Reopened (RKOI code review round 3 on `467d6ba`)**: `d5b518a` made the message text uniform, but the outcomes still differ — pending `receipt_id` (`conflict` vs `PENDING_INBOUND`), inbound `exchange_id`, `message_id` and `injection_id` (`conflict` vs success), and `inbound_message_id` (`thread_scope_denied` vs `PENDING_INBOUND`). It leaks only that some id exists somewhere, never content. `reply_to_message_id`, `source_event_id`, `record_id`, `supersedes_record_id` and `source_message_refs` do not leak | low / low | **Accepted for stage 1 (RKOI ruling)**: zuri-ai's ids are random and unguessable. Revisit with tenant-scoped uniqueness if any id ever becomes guessable or sequential. Related effect (`3d5ea92`): an inbound append no longer fails when a pending delivery can't reconcile because of a `receipt_id` collision. The pending row stays, is retried on every later drain, and each attempt writes a `reconcile_skipped` journal entry carrying a stable error code. This is bounded by the pending rows and harmless | KIN |
| RSK-MEMOS-10 | **New (RKOI stage-1 code review round 2 on code commit `95629e2`).** API-011's `outputSchema` (`API-011.tools.json`) is enforced by a contract test only, never at runtime — a handler bug producing a malformed response is not caught server-side, only by the test suite | low / low | Named here and in design §15; contract-test coverage is the current mitigation; runtime `outputSchema` validation is not scheduled against any gate yet. **Widened (RKOI code review round 3)**: only 2 of the 10 tools declare an `outputSchema` at all; `msp_thread_resolve`, `msp_thread_message_append`, `msp_thread_delivery_record` and `msp_thread_injection_record` — the responses zuri-ai reads — must declare one, validated by `tests/contract/api-011-output-schema.test.mjs`, before any activation (`BL-MEMOS-094`). Not a stage-1 merge blocker, since `test:cross-zuri` and the security suite exercise those fields today | RKOI |
| RSK-MEMOS-11 | **New (RKOI stage-2 review round 1, `f74ad0d`; keyring approved `fd8095f`).** Whether a tenant is present in `MSP_THREAD_SERVICE_KEYRING` is observable from the difference between `grant_unconfigured` (tenant absent) and `grant_signature_invalid` (tenant present, signature simply wrong) — a tenant-existence signal, the same shape of leak `RSK-MEMOS-09` already accepts for other ids | low / low | **Accepted, not fixed**: the trust boundary is stdio-only (design §6.1) — a caller able to reach this server at all already shares the process boundary that holds every tenant's actual keys, so learning "tenant X is provisioned" adds nothing a co-located caller could not already infer some other way | KIN |
| RSK-MEMOS-12 | **New (PH-MEMOS-5 scoping, `BL-MEMOS-105`'s cross-repo verification against zuri-ai's shipped `msp-vault-resolver.js`, `origin/main@4ca28c1d`).** zuri-ai's real, already-shipped `msp_vault_resolve` caller predates principal vaults entirely: it sends no `grant`/`signature` at all (an unsigned request, unlike every API-011 tool), has no `allow_passport` concept anywhere in its fixed `authorization` key set, and requires a `project_id` field that has no principal-vault meaning. None of these blocks the caller from working exactly as it does today once `msp_vault_resolve` exists (design §5.3.1's full verification table), but until `BL-MEMOS-113` lands, `principalPassportVaultId` is unreachable from zuri-ai (always resolves `null`, since `allow_passport` is never sent) and the new response fields are silently dropped by the shipped, unmodified `validateVaultSet` | certain / low (safe by construction — `DEC-MEMOS-40`/`41`/`42`'s additive resolutions mean the gap is "unreachable feature," not "broken caller" or "over-granted access") | Tracked as `BL-MEMOS-113` (the zuri-ai-owned caller update), deferred to `PH-MEMOS-8` alongside `BL-MEMOS-106`/`092`/`093` per owner direction (2026-09-14: no LINE OA yet, nothing production-facing needs this before then) | zuri-ai owner |

## 8. Traceability

| Epic (zuri-ai roadmap) | Phases | Backlog items |
|---|---|---|
| TASK-MEMOS-001 | PH-MEMOS-1, PH-MEMOS-7 | BL-MEMOS-010..014, BL-MEMOS-087 |
| TASK-MEMOS-002 | PH-MEMOS-2, PH-MEMOS-3 | BL-MEMOS-020..033, BL-MEMOS-040..049, BL-MEMOS-100 (cancelled), BL-MEMOS-102..104, BL-MEMOS-108..111 |
| TASK-MEMOS-003 | PH-MEMOS-4, PH-MEMOS-8 | BL-MEMOS-050..052, BL-MEMOS-057, BL-MEMOS-058, BL-MEMOS-092 |
| TASK-MEMOS-004 | PH-MEMOS-4, PH-MEMOS-8 | BL-MEMOS-053..057, BL-MEMOS-059, BL-MEMOS-093 |
| TASK-MEMOS-005 | PH-MEMOS-8 | BL-MEMOS-090, BL-MEMOS-091, BL-MEMOS-106, BL-MEMOS-107, BL-MEMOS-113 |
| TASK-MEMOS-006 | PH-MEMOS-8 | BL-MEMOS-094 |
| TASK-MEMOS-007 | PH-MEMOS-8 | BL-MEMOS-095 |
| TASK-MEMOS-008 | PH-MEMOS-5 | BL-MEMOS-060..068, BL-MEMOS-105 |
| TASK-MEMOS-009 | PH-MEMOS-6 | BL-MEMOS-070..075 |
| TASK-MEMOS-010 | PH-MEMOS-0, PH-MEMOS-7 | BL-MEMOS-002..005, BL-MEMOS-080..086, BL-MEMOS-088, BL-MEMOS-101 |

The zuri-ai roadmap orders the epics for the LINE agent: 001 → 002 → 003 → 004 → 005 → 006 → 007 → 008 → 009 → 010. This plan re-sequences them for an MSP-only build. Vaults (008) come before consolidation (009). Erasure is split: thread tables in PH-MEMOS-4, and vault extension in PH-MEMOS-6. The channel epics 005–007 are deferred. The epic ids and their meaning are unchanged.

## CHANGELOG

| Version | Date | Status | Summary | Commit Hash | Agent |
|---|---|---|---|---|---|
| 0.1.18b | 2026-09-16 | proposed | **PH-MEMOS-5 (principal vaults, API-010 `msp_vault_resolve`, API-009 `access_context` amendment, scoped `contexts` receipts, multi-agent vault rules) fully scoped for the first time — design pass only, not yet RKOI-reviewed**, against design v0.6.0b §5–§5.5/§12.4–§12.4.1 and the ADR's own new revision-note/decision-list entries. Rewrote `BL-MEMOS-060..068` in full with exact schema, tool shapes and named test files/cases, matching the density `BL-MEMOS-050..059` established for PH-MEMOS-4. Rewrote `GATE-MEMOS-5`'s five placeholder bullets into nine precise cases. Added **`DEC-MEMOS-36..48`** (13 new adopted defaults, pending owner confirmation — none of this range is confirmed yet, unlike every earlier `DEC-MEMOS` range in this table) and **`RSK-MEMOS-12`** (zuri-ai's shipped, unsigned `msp_vault_resolve` caller predates principal vaults — `allow_passport` absent, `project_id`'s foreign meaning — resolved additively, tracked via new backlog item **`BL-MEMOS-113`**, deferred to PH-MEMOS-8). Corrected `BL-MEMOS-106`'s stale `design v0.2.3b §5.1` citation to `v0.6.0b §5.1`. Updated the traceability table for `TASK-MEMOS-005` (`BL-MEMOS-113`). No id renumbered or reused; new ids: `DEC-MEMOS-36..48`, `BL-MEMOS-113`, `RSK-MEMOS-12`. | working-tree | ATHER |
| 0.1.17b | 2026-09-15 | proposed | Owner confirmed DEC-MEMOS-22..35 ("ยืนยัน"); status-only change, no decision text altered. | working-tree | ATHER |
| 0.1.16b | 2026-09-15 | proposed | RKOI PH-MEMOS-4 closure-round carry-forwards (non-blocking, folded per RKOI's instruction rather than a new review round): GATE-MEMOS-4's header and Leave bullet now name case 2b and case 3b (design §15 already had this; the plan bullet had not caught up); BL-MEMOS-053/054/055's version citations bumped to v0.5.3b, matching BL-MEMOS-050's round-4 fix. | working-tree | COORD |
| 0.1.15b | 2026-09-15 | proposed | **Answers RKOI's PH-MEMOS-4 review round 3, NEEDS REVISION 1 critical plus 5 warnings**, against design v0.5.3b. The critical was against the ADR's own decision-record paragraphs (fixed there, see the ADR's own CHANGELOG) — nothing in this plan needed a critical-level fix. **Two rows fixed (WARNING 5)**: `BL-MEMOS-050`'s stale `design v0.5.1b §7.1` citation bumped to `v0.5.3b`; `BL-MEMOS-052`'s test-list description, which previously named only the single rejoin case and the status-race case, expanded to all five required branch cases (never-joined, rejoin-with-no-claim refused, rejoin-with-`assertParticipants` succeeds, current-row `DEC-MEMOS-15` self-upgrade, and the new case 2b — a caller with `assertParticipants` can re-attach a *different*, departed third party on `GROUP`/`ROOM` threads, refused unconditionally on `DIRECT`) plus the assertion that the raw `SqliteError` code (now narrowed to exactly `SQLITE_BUSY_SNAPSHOT`, WARNING 2) never reaches a caller. `GATE-MEMOS-4`'s own design-version citation bumped to `v0.5.3b` to match. No new `DEC-MEMOS`/`BL-MEMOS`/`RSK-MEMOS` id this round. | working-tree | ATHER |
| 0.1.14b | 2026-09-15 | proposed | **Answers RKOI's PH-MEMOS-4 review round 2, NEEDS REVISION 2 critical (both subtler than round 1)**, against design v0.5.2b. **CRITICAL 1, still open**: `BL-MEMOS-058` re-specified as an explicit three-way branch (never-participated / participated-but-none-current / current-row-exists) — round 1's "no current row → no row at all" was under-specified and both literal readings of it failed (a bare swap throws on `current.personId` for `null`; a null-hardened swap falls into `DEC-MEMOS-15`'s self-upgrade exception, re-admitting a claim-free rejoin). Case 2 (the rejoin) now unconditionally requires `assertParticipants` and never reads `current` or falls through to `DEC-MEMOS-15`; a fourth required test case (DEC-15 self-upgrade regression) added. **CRITICAL 2, new**: `DEC-MEMOS-34` gains a second, independent, ANDed disqualifying condition — no `thread_messages` row with `speaker_kind NOT IN ('HUMAN', 'AGENT')` anywhere on the thread, since `OPERATOR`/`UNKNOWN` speakers post messages with no participant row at all, so a sole-ever-`HUMAN` `GROUP` thread with one `UNKNOWN`-speaker message wrongly qualified and leaked that speaker's content into both erasure-exemption and export (RKOI's reproduced probe). Rewrote `GATE-MEMOS-4`'s bullets, `BL-MEMOS-053..056`/`058`/`059` and `DEC-MEMOS-32`/`34`/`35` to match. **Warnings folded in**: the ordering rationale corrected from a false "closing first would break the result" claim to an honest defense-in-depth statement, with the acceptance case replaced by a property test (WARNING 1); the vacuous `dry_run` row-count assertion replaced with a `redaction_state`-count/content-column assertion (WARNING 2); the `close_for_relink` race's remaining raw `SQLITE_BUSY_SNAPSHOT` case mapped to a typed `conflict`, added to `BL-MEMOS-052`'s acceptance (WARNING 3); the `speaker_id`/`person_id` disqualifying check split into two separate counts, either of which disqualifies (WARNING 4); `RSK-MEMOS-06` gains a stated permanent-erasure-gap consequence, no new risk id (WARNING 5); `DEC-MEMOS-35`'s no-journal half withdrawn — `dry_run: true` now also journals, only the nonce exemption stays dry-run-specific (WARNING 6); `BL-MEMOS-059`'s migration now states its dependency on `0009`, not merely `0008` (WARNING 7). **No new `DEC-MEMOS`/`BL-MEMOS`/`RSK-MEMOS` id this round** — `DEC-MEMOS-34`/`35`, `BL-MEMOS-058`/`059` and `RSK-MEMOS-06` are revised in place; consistency re-checked, no duplicates. | working-tree | ATHER |
| 0.1.13b | 2026-09-15 | proposed | **Answers RKOI's PH-MEMOS-4 review, NEEDS REVISION 4 critical**, against design v0.5.1b. Rewrote `GATE-MEMOS-4`'s bullets to assert the actual fixes: rejoin-refusal (CRITICAL 1), no-content-not-"all-traces" wording, `scope_json` blanking, `thread_pending_deliveries` untouched, transaction ordering, the required GROUP-thread `DEC-MEMOS-34` reproduction (both directions), and retention's `dry_run`-no-nonce-no-journal/whole-tenant rulings. Rewrote `BL-MEMOS-050`, `052`, `053`, `054`, `055`, `056`, `057` to match; added new **`BL-MEMOS-058`** (CRITICAL 1's `thread-guard.mjs` rejoin-query fix, code correction to already-shipped `main`, not a `DEC-MEMOS` id) and **`BL-MEMOS-059`** (new migration `migrations/0010_erasure_receipts.sql`, provisional name, drop+recreate of `trg_protected_memory_records_update_guard` for `scope_json`, CRITICAL 4 item 1). Corrected `DEC-MEMOS-32`'s table row and added **`DEC-MEMOS-34`** (sole-ever-HUMAN-participant scoping for erasure/export summary and delivery-receipt disposition, a conservative under-erasure default, CRITICAL 2/3) and **`DEC-MEMOS-35`** (`dry_run: true` is fully read-only, WARNING 6). Extended `RSK-MEMOS-01`'s item 4 with the full generic thread-bound gate's caller shape for `close_for_relink` (WARNING 3), not only the two participant claims. Updated the traceability table for `BL-MEMOS-058`/`059`. No id renumbered or reused; no new `RSK-MEMOS` id. New ids: `DEC-MEMOS-34`, `DEC-MEMOS-35`, `BL-MEMOS-058`, `BL-MEMOS-059`. | working-tree | ATHER |
| 0.1.12b | 2026-09-15 | proposed | **PH-MEMOS-4 (participant lifecycle, erasure, retention, export) fully scoped for the first time**, against design v0.5.0b §7.1/§8.6/§11.2/§12.3. Rewrote `BL-MEMOS-050..056` in full with exact tool names, request/response shapes and per-decision rationale. Rewrote `GATE-MEMOS-4`'s bullets from generic placeholders into six concrete cases (relink, leave, detach, erasure idempotency, retention cross-tenant/`dry_run`, export scope). Added **`DEC-MEMOS-22..33`** (twelve new adopted defaults, pending owner confirmation): `leave`'s unconditional `assertParticipants` and never-closes-the-thread rule; `close_for_relink`'s `assertRelink` claim; agent detach needing no new claim; `data_subject_admin` resolved as the grant flag pair `dataSubjectAccess`/`dataSubjectAdmin` (closing the ADR's own open question); tool names `msp_thread_principal_erase`/`msp_thread_principal_export`/`msp_thread_retention_tick`; erasure idempotency-key shape and replay behavior; `erasure_receipts`' raw-`principal_id` storage convention; retention's deployment-wide `MSP_THREAD_RETENTION_DAYS` scope; export's tombstone-exclusion and agent-visibility-ignoring rules; erasure/export's per-table selection criteria; and the unknown-principal trivial-success rule. Updated `RSK-MEMOS-01`'s relink-caller item with `assertRelink`'s exact shape and `RSK-MEMOS-06` with `BL-MEMOS-054`'s partial mitigation — no new `RSK-MEMOS` id (every risk already tracked by `01`/`05`/`06`/`09`). Updated `BL-MEMOS-092`/`093` with their exact zuri-ai-side grant shapes. No id renumbered or reused; no new `BL-MEMOS` id (`050..057` already covered this scope). | working-tree | ATHER |
| 0.1.11b | 2026-09-14 | proposed | BL-MEMOS-010 now cites DEC-MEMOS-01..21 and RKOI rulings 1–4 as owner-confirmed (2026-09-14), and the current ADR version v0.1.11b. | working-tree | COORD |
| 0.1.10b | 2026-09-14 | proposed | Records the owner's confirmation of DEC-MEMOS-17..21 (2026-09-14): §6 intro and DEC rows; BL-MEMOS-014 and BL-MEMOS-087 cover 01..21. | working-tree | COORD |
| 0.1.9b | 2026-09-15 | proposed | **Folds RKOI's stage-2 review round 2 warnings after APPROVAL at commit `72e593f` (0 critical)**, against design v0.4.2b. **Supersession unified** (`BL-MEMOS-043`, `GATE-MEMOS-3`): round 1's own `thread_scope_denied` fix was itself a third oracle value; owner-direction ruling collapses unknown id / cross-agent `AGENT`-visibility / stage-1 ownership-status failure (previously `conflict`) into one identical `validation_failed` answer, checked existence-and-cross-agent first, then ownership/status. **`BL-MEMOS-112` corrected**: `trg_thread_pending_deliveries_update_guard` also drop+recreated to pin `agent_id`/`workspace_id` (the migration comment wrongly claimed the records guard was the only drop+recreate); drain re-check now targets the inbound message's own thread, not a freshly re-derived room `ACTIVE` thread; a legacy `NULL`-agent pending row is never drained (fails closed); both delivery paths' real `speaker_id` stated precisely (resolved: `grant.agentId`; drain: the stored `agent_id`; no hard-coded label anywhere). **`BL-MEMOS-048` corrected**: `resolve`'s nonce is consumed on every outcome (mint, attach, no-op), not only the mint path. **Wording fixes**: withdrew the wrong "no per-condition message variety" claim about the keyring (`BL-MEMOS-049`) — the code gives a distinct per-rule message, it just never names an id or key. **`BL-MEMOS-042` extended**: `msp_session_sweep`'s per-job metadata gains `thread_kind`/`channel_type` so the worker can construct its own resolve call; the worker's resolve grant sources `audienceKind` from its Tier-1 room config. **`GATE-MEMOS-2` extended**: states the `test:cross-zuri` pre-/post-`BL-MEMOS-107` flip explicitly, matching `GATE-MEMOS-3`'s existing wording, since both gates name the same suite and must both stay meetable against `main` once stage 2 merges. Added **`DEC-MEMOS-21`** (`agentId`/`workspaceId` bounded at 128 characters), promoted from unnumbered prose, to the decisions table and §19 framing. No id renumbered or reused. | working-tree | ATHER |
| 0.1.8b | 2026-09-15 | proposed | **Answers RKOI's stage-2 review round 1 on commit `f74ad0d` (NEEDS REVISION, 2 critical)**, against design v0.4.1b. **CRITICAL 1** (new `BL-MEMOS-112`): the delivery pending path stored no agent, letting `#drainDeliveries` mint an `OUTBOUND` message under a hard-coded `speakerId`; fixed via `thread_pending_deliveries.agent_id`/`workspace_id`, a new `ThreadRegistry#findThreadByRoom` lookup, and a drain-time agent re-check. **CRITICAL 2** (`BL-MEMOS-043`): `record_id`'s dedup hash gains `agent_id`/`visibility`; cross-agent supersession refused `thread_scope_denied` (later found itself an oracle and unified in round 2, above). **`BL-MEMOS-049` rewritten** to match the code: JSON-object-only keyring, `thread_keyring_config_invalid` startup refusal, no-prototype map, dedicated `tests/security/thread-service-keyring.security.mjs` suite (later finalized by RKOI's approval at `fd8095f`, incorporated into this same 0.1.8b version rather than a separate bump, since no new version was requested for that follow-up). **`BL-MEMOS-048` corrected**: nonce insert moves from the guard into each store method's own transaction, since `thread-guard.mjs`'s handlers are `async` and better-sqlite3 refuses an async transaction; pruning always uses the server clock. **`DEC-MEMOS-18` revised**: worker attaches via `assertAgents`, never mints from a worker-only grant; withdrew the wrong "ending a `thread_agents` row is revocation" claim. Added **`DEC-MEMOS-19`** (default visibility `THREAD`) and **`DEC-MEMOS-20`** (nonce: ≥128 random bits, `(tenant_id, nonce)` key, 200-row bounded prune). Deduplicated `BL-MEMOS-090`/`107` (transport vs. grant-shape ownership); added the outbound-`agentId`-matches-`speakerId` cross-repo item; added `RSK-MEMOS-09`-adjacent `RSK-MEMOS-11` (keyring tenant-membership oracle, accepted). Rewrote `GATE-MEMOS-3`'s bullets to match every fix above. Removed the leftover "confirm the nonce gap/DEC-15/DEC-16" asks from §19 now that `01..16` are confirmed. No id renumbered or reused. | working-tree | ATHER |
| 0.1.7b | 2026-09-15 | proposed | **PH-MEMOS-3 stage-2 (multi-agent) spec, fully scoped for the first time** (owner direction 2026-09-14: proceed with the next planned work), against design v0.4.0b. Rewrote `BL-MEMOS-040..049` in full: `BL-MEMOS-040` (grant's `agentId`/`workspaceId` extend `verifyThreadGrant`'s existing required-claim check, reusing `grant_signature_invalid`; journal actor becomes `grant.agentId` in plain text, replacing the shipped `workspaceId: job.tenant_id` placeholder); `BL-MEMOS-041` (`thread_agents` DDL, attachment exactly two ways — auto-attach on `created: true`, or `assertAgents` on an existing thread, resolve-only); `BL-MEMOS-042` (agent gate per tool, `agent_not_current` new); `BL-MEMOS-043` (`protected_memory_records` gains `agent_id`/`visibility` via `ALTER TABLE`, no rebuild, plus the required drop+recreate of `trg_protected_memory_records_update_guard`); `BL-MEMOS-044` (summaries need no new column — the agent gate alone fully gates a departed agent's reads); `BL-MEMOS-048` (nonce required on every mutating tool except `msp_thread_message_append` and the read-only `msp_thread_context`, two new codes `grant_nonce_required`/`grant_replayed`, same-transaction insert); `BL-MEMOS-049` (`MSP_THREAD_SERVICE_KEYRING` format: JSON object or file path, ≥32-char values, no fallback once configured, fail-closed per tenant, not per deployment). Every row now also depends on `BL-MEMOS-013`/`BL-MEMOS-033` explicitly, since stage 2 is built stacked on the unmerged stage-1 branch. Added **`DEC-MEMOS-17`** (no zuri-ai compatibility flag — activation already gated behind `BL-MEMOS-090`) and **`DEC-MEMOS-18`** (the worker signs as the thread's own agent; `claim`/`commit`/`retry` require agent-currency on the job's thread; `sweep` alone is exempt from "current"), both new adopted defaults pending owner confirmation, to the decisions table. Rewrote `GATE-MEMOS-3`'s five placeholder bullets into nine precise cases matching design §15's new stage-2 invariant rows. Updated `BL-MEMOS-107`/`RSK-MEMOS-01` with `DEC-MEMOS-17`'s no-compatibility-flag note directly on the `agentId`/`workspaceId`/`nonce` cross-repo item. Rewrote the `## 6. Decisions` intro: `DEC-MEMOS-01..16` are confirmed by the owner (2026-09-14, "ยืนยัน", commit `214a7d2` on `docs/memos-001-adr-design-plan`), not pending — `BL-MEMOS-014`/`BL-MEMOS-087` are done for that range (recorded on the other branch, not duplicated here). No id renumbered or reused. | working-tree | ATHER |
| 0.1.6b | 2026-09-14 | proposed | Records the owner's confirmation of DEC-MEMOS-01..16 (2026-09-14, no amendments): §6 heading and intro, the identifier table, GATE-MEMOS-1 and GATE-MEMOS-7 bullets, BL-MEMOS-010; BL-MEMOS-014 done; BL-MEMOS-087 done for DEC-01..16, with ADR acceptance at BL-MEMOS-013's merge. | working-tree | COORD |
| 0.1.5b | 2026-09-15 | proposed | Folds RKOI's stage-1 code review round 2 (commit `445bd90`, spec items against the docs, not a docs re-review). **Widened `BL-MEMOS-111`, CRITICAL on the code, KIN fixing it**: the shipped guard's room-hash comparison only runs `if (grant.externalRoomRef)`, so a grant with no room claim at all currently skips the check and passes on tenant/business/account alone; fixed to require the claim outright (`thread_scope_denied` when absent) on all eight thread-bound tools plus `sweep`; added the "no room claim" case to `GATE-MEMOS-2`'s bullet list and `BL-MEMOS-111`'s acceptance column. Added **DEC-MEMOS-16** (a `channel_type` mismatch against an existing `ACTIVE` thread's stored value, same tenant/account/room hash, is refused `conflict`, replacing the design's "regardless of transport label" claim) to the decisions table, `GATE-MEMOS-2`, `BL-MEMOS-014`'s checklist and `BL-MEMOS-010`; updated every live `DEC-MEMOS-01..15` reference to `01..16`. Extended **`BL-MEMOS-102`** with six more schema items KIN is adding to `0008` directly: an `exchange_id`-leading index, typed errors with no foreign-tenant existence oracle on the `exchange_id`/`reply_to` triggers, `thread_summary_invalidations` fully immutable, an injection's `exchange_id`-belongs-to-its-thread check, `threads` not deletable, and delivery receipts referencing `OUTBOUND` messages only. Corrected §9.2's `msp_session_sweep` claim from "stays tenant-scoped" to room-scoped (confirmed against `thread-guard.mjs:274-280`, which overwrites `channel_account_id`/`external_room_ref` from the grant same as `tenant_id`/`business_id`) and added sweep's own "no room claim" refusal case to `GATE-MEMOS-2`. Replaced the wrong "`BL-MEMOS-033` confirms zuri-ai's worker grant carries `audienceKind`" claim with the fact that zuri-ai has no `msp_session_*` caller at all (confirmed against `origin/main@1ddccb70`) — the only worker is MSP's own `thread-summary-worker.mjs`. Added **RSK-MEMOS-09** (foreign-tenant `receipt_id` existence oracle on `thread_pending_deliveries`, low severity, KIN may close it) and **RSK-MEMOS-10** (`outputSchema` enforced by a contract test only, never at runtime) as named stage-1 gaps, tracked rather than silently accepted. Updated `BL-MEMOS-012`'s round-tracking row, `RSK-MEMOS-03` (the room-claim gap is exactly the prose/inference-vs-code drift this risk names, found only by reading `thread-guard.mjs` directly), and every stale `v0.3.4b`/`v0.1.4b` "current version" pointer in this plan to `v0.3.5b`/`v0.1.5b`, while leaving historical CHANGELOG and round-tracking text describing what earlier rounds actually said unchanged. No id renumbered or reused. | working-tree | ATHER |
| 0.1.4b | 2026-09-15 | proposed | Folds RKOI's nine round-four warnings (docs **APPROVED, 0 critical**, commit `1c4a62f`) ahead of merge. Added **`BL-MEMOS-111`**: the room-hash check must run on every thread-bound call including `claim`/`commit`/`retry` via the job's own thread — a cross-room gap with no prior backlog row, since `msp_session_compaction_claim` had no scope check of any kind. Made it a `BL-MEMOS-033` dependency and added a `GATE-MEMOS-2` bullet. Corrected `BL-MEMOS-102`'s `thread_summary_invalidations` diagnosis (the old `DEFAULT ''` refused the mismatched insert via the trigger, it did not succeed silently; the real bug is `INSERT OR IGNORE` swallowing a `NOT NULL` violation once the column is `NOT NULL`, fixed by `ON CONFLICT(summary_id) DO NOTHING` and an `IS NOT` comparison) and added the "at most one `OPEN` session per thread" invariant (never a claim about `CLOSING`, conditional on `BL-MEMOS-033` proving every flow preserves it). Rewrote `BL-MEMOS-021` (no `thread_bindings` table to restore — contradicted `BL-MEMOS-100`'s own cancellation) and `BL-MEMOS-023` (assurance upgrade per DEC-MEMOS-15, not "only via the lifecycle tool"); scanned `BL-MEMOS-020..033` as a block and fixed stale `v0.3.1b`/`v0.3.2b` version pins. Changed `BL-MEMOS-109`'s audience rule from "check only when present" to **`audienceKind` required on every thread tool except `msp_thread_delivery_record`**, per owner direction; added the `docs/API-011-THREAD-MEMORY-CONTRACT.md:54,196` and cross-test-header update to its scope. Reworded `BL-MEMOS-110`: the script and the test already exist, this item is about making `test:cross-zuri` pass and gating `GATE-MEMOS-2` on it. Stated the `personId`-change lock-up mechanism directly in `RSK-MEMOS-01`'s own row (previously only pointed at from elsewhere without actually being recorded there). Named suite files on `GATE-MEMOS-4/5/6`. Removed every citation of RKOI's session-scratch probe scripts as evidence, replacing each with the finding itself or the backlog item whose acceptance test proves it. No id renumbered or reused. | working-tree | ATHER |
| 0.1.3b | 2026-09-14 | proposed | Answers RKOI's round-three NEEDS REVISION on commit `6d1a801` (1 critical). Added **DEC-MEMOS-15** (assurance self-upgrade needs no `assertParticipants` under four conditions; a downgrade is silently ignored) to the decisions table. Widened **BL-MEMOS-109** to cover both the audience-check skip and the removal of the `channelType` grant requirement, with an acceptance test using zuri-ai's exact delivery claims on both the inbound-already-seen and inbound-not-yet-seen paths, plus `npm run test:cross-zuri`. Added **BL-MEMOS-110** (the `tests/cross/zuri-thread-contract.test.mjs` cross-repo harness itself, reading a real `origin/main` extract via `MSP_TEST_ZURI_ROOT`) and made it a dependency of `BL-MEMOS-033` and `BL-MEMOS-109`. Extended **BL-MEMOS-102** with the additional consistency-trigger gaps RKOI's probes found (`thread_summary_invalidations.tenant_id` NOT NULL with no default, not a `DEFAULT ''`; `session_compaction_jobs`/`session_summaries`/`protected_memory_records` session-belongs-to-thread checks; a `thread_participants` tenant trigger that never existed; a delivery-reconcile-after-session-close acceptance case) and **BL-MEMOS-108** (the injection trigger must also pin `injection_id` itself, not only the other columns). Rewrote **RSK-MEMOS-01**: removed the sentence claiming `assertParticipants` "needs no zuri-ai change" (it contradicted items 4 and 5); item 5 (assurance-upgrade caller) is now stated as resolved MSP-side by DEC-MEMOS-15 with no BL id needed; item 4 (relink/merge caller) is unchanged and mapped to the existing zuri-owned `BL-MEMOS-092`. Kept **RSK-MEMOS-03** open — three rounds of prose/inference-vs-code drift, not two. Fixed `GATE-MEMOS-1`'s stale "design v0.3.0b" reference and `GATE-MEMOS-7`'s stale "`DEC-MEMOS-01..10`"; named concrete suite files (`thread-memory-scoping.security.mjs`, `thread-agent-scoping.security.mjs`) on `GATE-MEMOS-2`/`GATE-MEMOS-3`. No id was renumbered or reused; `BL-MEMOS-100` remains cancelled. | working-tree | ATHER |
| 0.1.2b | 2026-09-14 | proposed | Answers RKOI's round-two NEEDS REVISION on commit `92cb591` (1 critical, plus checks and warnings). Read the shipped stage-1 code (`feat/memos-002-thread-memory`) directly. **Cancelled BL-MEMOS-100** (`thread_bindings` restoration) — the shipped `0008` never had a separate binding table to restore; binding columns already live on `threads`, and identity-key rotation is an accepted stage-1 gap (design v0.3.2b §6.2), not something BL-MEMOS-100 could have fixed. Corrected BL-MEMOS-102's table list (it wrongly named `thread_bindings`/`exchanges`, neither of which exists) to the real gaps: `thread_summary_invalidations`'s missing `tenant_id`, `chat_sessions`'s missing UPDATE-pin trigger, and `thread_messages`'s missing cross-table consistency checks. Marked BL-MEMOS-103 `done` (already shipped) and moved BL-MEMOS-101 (`msp_ping`) out of GATE-MEMOS-2 into PH-MEMOS-7, confirmed against the code that stage 1's `msp_ping` does not report `identity_surface`. Added BL-MEMOS-108 (injection state-machine trigger) and BL-MEMOS-109 (delivery audience-check exemption — a real refusal bug in the shipped guard) as newly confirmed code gaps. Narrowed BL-MEMOS-107 to stage-2 flags only, since `assertParticipants` is already shipped. **Reopened RSK-MEMOS-03**, since this is the second round in which a design revision did not match the shipped code — closed only when `BL-MEMOS-033`'s own code review closes it, not by a design revision. Extended RSK-MEMOS-01's tracked list with the relink and assurance-upgrade callers RKOI asked be added. Pointed `participant-lifecycle-relink.security.mjs` at BL-MEMOS-052 explicitly, consolidating relink cases into one suite file rather than splitting them. | working-tree | ATHER |
| 0.1.1b | 2026-09-14 | proposed | Answers RKOI's NEEDS REVISION on commit `2f4d584` (3 critical findings, all in the ADR/design pair, plus warnings and new decisions touching this plan directly). Added DEC-MEMOS-11..14 to the decisions table and corrected DEC-MEMOS-07's migration-order wording (numbers assigned at merge, not pre-bound to `0009`, per DEC-MEMOS-14). Rewrote RSK-MEMOS-01 with the full cross-repo change list (`agentId`/`workspaceId` required, `nonce`, `assertAgents`, `assertParticipants`) and closed RSK-MEMOS-03 as resolved (the design/code divergence it warned about is exactly what RKOI's review caught). Added BL-MEMOS-012 as an explicit dependency of BL-MEMOS-021..024, 029 and 030, and updated those items' descriptions to match the corrected design. Added BL-MEMOS-100..107 (numbering convention extended to 100+ where a phase's block was already full, per the amended §2): `thread_bindings`/rotation, `msp_ping` identity-surface diagnostic, tenant-consistency triggers, the contracts no-SQL structural test, opportunistic nonce pruning, cross-repo verification of zuri-ai's `msp-vault-resolver.js` against API-010, and the two zuri-ai-side cross-repo changes (API-009 `access_context`, and the full grant-shape/nonce/assertion changes). Reconciled BL-MEMOS-051's agent-detach wording with design §8/§13. Updated the traceability table for all of the above. | working-tree | ATHER |
| 0.1.0b | 2026-09-14 | proposed | Initial implementation plan PLAN-MSP-MEMOS: identifier scheme; nine phases PH-MEMOS-0..8 with gates GATE-MEMOS-0..8; sprints SPR-MEMOS-00..09+; backlog BL-MEMOS-001..096 (including BL-MEMOS-048 grant nonces and BL-MEMOS-049 per-tenant keyring from the ADR) mapped to epics TASK-MEMOS-001..010; decisions DEC-MEMOS-01..10 as adopted defaults pending confirmation; risks RSK-MEMOS-01..08; channel activation deferred by owner direction. | working-tree | Claude Opus 5 |
